# AoM Sports Club Platform — Consolidation & CRM Build Plan

> This document is the working plan for Claude Code. Read it fully before starting any task.
> Work through phases in order. Do not start a task whose dependencies or open questions are unresolved.
> Update the Status column as you go and record decisions in `DECISIONS.md`.

---

## 1. Context

The club currently runs four separate systems:

| System | Where | Notes |
|---|---|---|
| Pitch booking + early CRM | **Neon** (Postgres) | To be migrated and decommissioned |
| Function room booking + staff ops | **Supabase** project `aomsc-function-room` (`rwpglslbkhsqyxjhnpue`) | Live data; becomes the base project. Already contains: profiles, room_bookings, booking_payments, booking_emails, audit_log, timesheets, holiday_requests, sickness_records, clubhouse_projects, clubhouse_checklists, site_settings, email_templates |
| Fixtures | Supabase project `fixtures-system` (`boeaggxhkxbzuiojlbqi`) | **Unrelated standalone system — never touch it.** Fixtures for this platform are imported from **FA Full-Time** (fulltime.thefa.com), see P2.3–P2.5. |
| USC-Sale | Supabase project `nxmtjolusgeblmgucptn` | **Out of scope** (decision 2026-08-21). Do not touch. |

**End state:** one Supabase project (`aomsc-function-room`, to be renamed) holding the entire club platform: unified bookings (pitches + rooms), full CRM (members, teams, seasons, payments, safeguarding, media), WhatsApp-style messaging, staff ops. One Next.js web app, one Expo mobile app (iOS + Android), one monorepo.

**Stack (decided):** Supabase (Postgres 17, Auth, Realtime, Storage, Edge Functions), Next.js 14+ on Vercel, Expo/React Native with EAS, SumUp (Adam 2026-08-23: not Stripe — see DECISIONS D-SumUp; P4.1 currently Stripe-shaped, rework pending), Resend, Twilio (fallback SMS), Cloudinary or Supabase Storage for media (decide in Phase 4).

---

## 2. Hard rules (apply to every task)

1. **Never run destructive SQL against the production project directly.** All schema changes go through Supabase CLI migrations in the repo, rehearsed on a Supabase branch first.
2. **RLS on every table, written with the table, not after.** A migration adding a table without policies fails review.
3. **A written review before merge** on any PR touching: member/person data, safeguarding module, messaging, auth, payments, or RLS policies. Adam handed the merge itself to Claude on 2026-09-01 — *"you merge it for me. Change the rules if you need to"* — after a run of PRs sat green and waiting on a click. What was never the point of this rule is the click; what was the point is that somebody looks. So these PRs no longer wait, and in exchange each one must say **in its own body**, under the four §11 headings, what it touches and why it is safe — and Claude does not merge one until that has actually been written and is true. An empty "RLS: none" on a PR that changes a policy is the failure this rule exists to catch. (This applies to Finn-loop `finn-review` too.)
   - **Still stops for Adam:** a PR that weakens a §2.4 safeguarding invariant. SAFEGUARDING.md §6.2 requires a weakening to be recorded with its reason and signed off, and a signature is not something a delegation can hand over.
4. **Safeguarding invariants are enforced in the database** (RLS/constraints/triggers), never only in application UI:
   - No conversation may contain exactly one adult and one minor with no guardian participant.
   - Messages are soft-deleted only; no hard deletes of messages or audit rows.
   - Safeguarding concern records readable only by `safeguarding_lead` and `club_admin` roles.
5. **Migrations are additive during the transition.** Legacy tables are only dropped in the explicit decommission tasks, after sign-off.
6. **Secrets** live in Vercel/EAS/Supabase env config, never in the repo. `.env.example` documents every variable.
7. Generated DB types (`supabase gen types typescript`) are regenerated in the same PR as any migration.
8. Every phase ends with a working, deployable system. No task may leave main in a broken state.

---

## 3. Decisions & remaining unknowns (resolved with Adam, 2026-08-21)

| # | Item | Decision |
|---|---|---|
| Q1 | Neon app auth mechanism | **Unknown to Adam.** Discover it in P3.1: inspect the Neon codebase and schema for Clerk tables/SDK usage, a local `users`/`sessions` table with password hashes, NextAuth tables, etc. Document findings in `MIGRATION_MAP.md` and get Adam's sign-off on the mapping approach before P3.2. |
| Q2 | Fixtures | **`fixtures-system` is an unrelated standalone project — never migrate, modify, or integrate with it.** The platform gets its own fixtures module (main project), populated automatically from **FA Full-Time**. The FA provides no official API; import is via scraping/parsing Full-Time pages using stored team/division identifiers (the approach used by Teamo and community libraries). Treat as an unofficial integration: isolate the parser, monitor for breakage, and always keep a manual paste/CSV fallback working. Fixtures + availability + pitch allocation are **in scope for mobile v1**. |
| Q3 | `USC-Sale` project | **Fully out of scope.** Never touch it. |
| Q4 | Media storage | **Supabase Storage** (with image transforms + signed URLs). Cloudinary not used. |
| Q5 | Cutover timing | **ASAP** — cutover proceeds as soon as the P3.3 rehearsal and reconciliation pass cleanly. Still requires a scheduled write-freeze window (a quiet weekday evening) and the P3.4 go/no-go checklist; "ASAP" does not mean skipping the rehearsal gate. |
| Q6 | Safeguarding framework | **Cheshire FA safeguarding requirements** (the FA Safeguarding Children Policy and guidance as applied by Cheshire FA). P5.1's spec must cite the specific rules it encodes; Adam verifies against current Cheshire FA published guidance at spec time. |

---

## 4. Phase 0 — Repo & environment foundation

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P0.1 | Create monorepo (Turborepo + pnpm): `apps/web`, `apps/mobile`, `packages/shared`, `packages/db` (migrations + generated types), `supabase/` (CLI config) | Repo builds; CI runs lint + typecheck + tests on PR | ☑ 2026-08-21 — scaffold builds; `turbo lint typecheck test` green locally; CI workflow added (first PR will prove it) |
| P0.2 | Link Supabase CLI to project `rwpglslbkhsqyxjhnpue`; pull current schema into versioned migrations as baseline | `supabase db diff` is clean against prod; baseline migration committed | ☑ 2026-08-22 — baseline `supabase/migrations/20260821000000_baseline.sql` reconstructed from live prod catalogs; types regenerated; CLI linked; `migration repair --status applied 20260821000000` on prod; history in sync. Verified by executing the baseline on a throwaway preview branch (`baseline-verify`, deleted after) and diffing catalogs vs prod: columns/constraints/indexes/triggers/policies/RLS/grants/sequences all identical. Docker Desktop installed (needed VT-x enabling in BIOS); `supabase db diff --linked -s public` run: after making grants explicit in the baseline, the only output is 5 `create or replace function` statements caused by CRLF bodies on prod (dashboard-authored). No-op migration `20260822000000_normalise_function_line_endings.sql` re-saves them with LF; pushed to prod with Adam's approval 2026-08-22; `supabase db diff --linked -s public` is now EMPTY. Acceptance criteria met. |
| P0.3 | Set up CI (GitHub Actions): typecheck, tests, `supabase db lint`, migration dry-run against a branch DB | CI green on a no-op PR; required checks configured | ☑ 2026-08-22 — ruleset `main` active: PR required, both checks required, deletion/force-push blocked. `.github/workflows/ci.yml` has two jobs: `verify` (lint/typecheck/test) and `database` (replays all migrations + seed on a fresh local Postgres, `db lint --fail-on error`, and `db push --dry-run` against prod when `SUPABASE_ACCESS_TOKEN`/`SUPABASE_DB_PASSWORD` secrets exist). PR #1 green on both jobs and merged; repo secrets added; run 32560047110 on `main` green incl. prod dry-run ("Remote database is up to date"). Phase 0 complete except P0.4 deploy smoke test (deferred with Vercel repoint). |
| P0.5 | Create `DECISIONS.md`, `SAFEGUARDING.md` (invariants from §2.4 expanded), and Linear project structure per phase | Docs exist; Linear issues created for Phase 1 | ☑ 2026-08-22 (PR #3 merged; four Codex review findings incorporated; open decisions D1–D9 + citations tracked as Linear TH1-16) — `SAFEGUARDING.md` written at repo root: expands §2.4 into invariants SG-0 (definition of minor; unknown DOB fails closed) and SG-1…SG-8, each with statement, guidance area, enforcement layer (constraint/trigger/RLS), the violation-attempting pgTAP test that must exist, and the implementing task. Also carries a citation table for the FA / Cheshire FA rule areas (per §3 Q6) with `[Adam to verify]` placeholders, a data-model checklist for Phases 1/2/4/5, 9 open decisions, and a change-control section tied to §2.3. AWAITING ADAM: review of the 9 open decisions and completion of the citation table — no SG-invariant is signed off until its citation row is filled in; P5.1 is blocked on this. `DECISIONS.md` created (lands on `main` with PR #2). Linear workspace `th1878` connected 2026-08-22: projects "Phase 0"…"Phase 6", labels `migration`/`rls`/`safeguarding`/`human-review`, issues TH1-5…TH1-9 (P0.1–P0.5, with real status) and TH1-10…TH1-15 (P1.1–P1.6, fully specified, dependency-chained; P1.1 blocked by P0.5 sign-off). REMAINING: Adam's review of SAFEGUARDING.md open decisions + citations. |
| P0.4 | Import existing function-room app code into `apps/web` (or link as first app if it stays separate short-term — confirm with Adam) | Existing room booking works unchanged from monorepo deploy | ☑ 2026-08-23 — deploy smoke test passed with the P1.6 cutover (`club-management-web`, `portal.aomsportsclub.co.uk`). Earlier note: ◑ 2026-08-22 — lift-and-shift of `AoM-Sports-Club-Function-Room` into `apps/web` complete. Imported its live `src/` tree unchanged (85 files: 58 `src/app`, 9 `src/components`, 17 `src/lib`, `middleware.ts`) plus `public/`, `docs/`, `vercel.json` (cron `/api/cron/payment-reminders` daily 09:00), `tailwind.config.ts` (incl. typography + animate plugins) and `postcss.config.mjs`; `next.config.mjs` merged (source's `serverActions.bodySizeLimit: 6mb` + monorepo's `transpilePackages`). The source repo's root `app/`/`components/`/`lib/` are empty dead leftovers and its `supabase/migrations` are historical (already in our baseline) — neither imported. No data-access refactor: `src/lib/types.ts` is hand-written domain interfaces, not a copy of the generated `Database` type, so there was nothing trivially drop-in to swap for `@club/db`. Deps merged at source versions (`stripe` pinned to `22.2.0` to keep its `apiVersion` literal valid); `recharts`/`resend`/`tsx`/`dotenv`/`@types/nodemailer` dropped as unreferenced (`scripts/` does not exist in the source). Lint moved off legacy `next lint` onto the repo flat config; two `src/**`-scoped relaxations documented inline in `apps/web/eslint.config.mjs` (`no-unused-vars` → warn, `no-unused-expressions` allows ternary/short-circuit) and `noUncheckedIndexedAccess: false` in `apps/web/tsconfig.json`. Env fully documented in `apps/web/.env.example` + root `.env.example`; `turbo.json` build env extended with `NEXT_PUBLIC_SITE_URL`/`NEXT_PUBLIC_VAPID_PUBLIC_KEY`. Verified: `pnpm turbo run lint typecheck` 8/8 green, `pnpm test` 4/4 green, `pnpm --filter @club/web build` green with placeholder Supabase env (no real secret needed at build time). REMAINING: (a) Vercel repoint — deliberately deferred per DECISIONS.md 2026-08-22, the live site still deploys from the old repo; when cut over, set root directory to `apps/web`, carry all env vars from `apps/web/.env.example`, and re-add the cron from `apps/web/vercel.json`; (b) smoke-test a real deploy against `rwpglslbkhsqyxjhnpue` before the acceptance criterion ("works unchanged from monorepo deploy") can be ticked; (c) archive/retire the `AoM-Sports-Club-Function-Room` repo once the repoint is done. |

---

## 5. Phase 1 — Unified core schema (the member model)

New tables land alongside existing ones; nothing is dropped.

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P1.1 | `people` — every human the club knows (players incl. minors, parents, coaches, staff, hirers). Fields: names, DOB, contact (nullable), `is_minor` derived/generated from DOB | Migration + RLS + types; unit tests for minor derivation | ☑ 2026-08-22 (pushed to prod 2026-08-22) — `supabase/migrations/20260822100000_people.sql` creates `public.people` (names mandatory + non-blank checks, nullable `dob`/`email`/`phone`, single `address jsonb` with an object check, `notes`, `legal_hold` + `pseudonymised_at` per SAFEGUARDING §4 SG-8, `created_at`/`updated_at`/`created_by`, soft delete via `deleted_at`/`deleted_by`), index `(last_name, first_name)`, and a partial unique index on `lower(email)` where the row is live. SG-0 implemented exactly: `is_minor_dob(date)` and `is_minor(uuid)`, both STABLE (the latter SECURITY DEFINER, `search_path = public`), NULL dob ⇒ minor, unknown/NULL person_id ⇒ minor, 18th birthday ⇒ adult, 29-Feb majority on 1 March in non-leap years. SG-2 guard pattern applied to `people`: generic `deny_hard_delete()` + `deny_truncate()` functions (named as SAFEGUARDING §4 does, so P4.3/P5.2 reuse them), both triggers, `revoke delete, truncate` from anon/authenticated/**service_role**, and no FOR DELETE policy. "dob not in the future" is `people_dob_guard()` (a CHECK cannot call `current_date`) — the *single* dob trigger §4 mandates, which P2.1 (SG-6 1c) and P5.2 (SG-1.2) must extend rather than duplicate. RLS enabled with three `is_committee()` policies (read/insert/update); grants reduced to select/insert/update for authenticated + service_role, nothing for anon; execute on both helpers to authenticated + service_role only (anon excluded — `is_minor` would otherwise be an existence-and-age oracle). **DEVIATION needing Adam's decision:** the task asked for "staff = read" but `is_staff()` covers bar/clubhouse staff, and SAFEGUARDING §1.3 gives that role "no inherent access to member or child data" — so no staff read policy was written; P1.4 adds the scoped reads via `has_role()`. Self-read left as a marked TODO (needs `profiles.person_id`, P1.2). pgTAP tests in `supabase/tests/` (3 files, **70 assertions, all green**): `people_is_minor.test.sql` (20), `people_rls.test.sql` (31, delete/truncate attempted as authenticated, service_role and the table owner), `people_constraints.test.sql` (19). pgTAP installed via `seed.sql` into `extensions`, never pushed to prod; CI gained a `supabase test db` step and its `db lint` is now `--schema public` (unscoped it fails on pgTAP's own function bodies). Verified locally in Docker: `db reset --local` → `test db` 70/70 PASS → `db lint --local --schema public --level error --fail-on error` clean. Types regenerated (`packages/db/src/database.types.ts`, purely additive, header + `__InternalSupabase` block preserved); `pnpm turbo run typecheck lint` 8/8 green. Written up in `docs/migrations/P1.1-people.md`. Rehearsed 2026-08-22 on preview branch `p1-1-people` (prod-shaped schema): `db push` applied cleanly, `db lint` clean, pgTAP 70/70 after one fix — hosted default ACL grants anon EXECUTE on new functions explicitly, so the migration now revokes from `anon` by name (lesson for all future SECURITY DEFINER helpers). Branch deleted. Merged as PR #4 (`044da16`). Prod push batched with P1.2 by Adam's decision (see DECISIONS.md): **pushed to prod 2026-08-22** together with `20260822110000_profiles_person_link.sql`; migration history 4/4 in sync, `supabase db diff --linked -s public` empty, prod holds 39 profiles / 39 people. |
| P1.2 | Rework `profiles` to link `auth.users` → `people` (one-to-one). Backfill: every existing profile gets a person row | All 38 existing profiles linked; room booking app unaffected | ☑ 2026-08-22 (pushed to prod 2026-08-22) — `supabase/migrations/20260822110000_profiles_person_link.sql` adds `profiles.person_id uuid not null unique references public.people(id) on delete restrict` (additive only: no existing column dropped or renamed, so apps/web's `role`/`full_name`/`bar_*`/`holiday_allowance_days` reads are untouched). Backfill is factored into `public.backfill_profiles_people()` returning `(profiles_linked, people_created, people_ids)` — idempotent by construction (`where person_id is null`), creating one `people` row per unlinked profile with names from `public.split_person_name(full_name)` (last token = surname, rest = forenames, single token ⇒ `last_name = '(unknown)'` because `people_last_name_not_blank` forbids `''` and duplicating the token would fabricate a surname), `email` copied defensively from `auth.users.email` (dropped to NULL on collision/format failure rather than aborting), `dob` null (SG-0: nobody is presumed adult for having had a login), `created_by` null, `created_at` inherited from the profile. The migration runs it, writes one `audit_log` row (`migration.backfill` / `profiles`, `actor_id` null, `actor_email` `'migration'`, `detail` = `{migration, profiles_linked, people_created, people_ids}`), then asserts `count(profiles where person_id is null) = 0` and `count(people) >= count(profiles)` in a `raise exception` guard, and only then sets NOT NULL. `handle_new_user()` replaced (same signature, SECURITY DEFINER, pinned search_path, same `on conflict (id) do nothing`): creates the person first, then the profile carrying its id. **NO AUTO-LINK BY EMAIL** — a new login never adopts an existing person sharing its email, because families share addresses and a child's record may carry a parent's; a duplicate person is a tidy-up, a merged one is a safeguarding incident (DECISIONS.md 2026-08-22). New `before insert on profiles` trigger `trg_profiles_ensure_person` is load-bearing, not decoration: Postgres checks NOT NULL against the proposed tuple **before** arbitrating `on conflict`, verified locally, so without it the three apps/web upserts that follow `auth.admin.createUser()` (`book/actions.ts:25`, `super-users/actions.ts:118`, `settings/actions.ts:225`) would all fail 23502 — the trigger adopts the existing profile's `person_id` (no orphan per upsert) or manufactures one. P1.1's deferred `people_self_read` policy landed; **no** self-update, deliberately (SG-0 makes `dob` an administrative field). Tests: `supabase/tests/profiles_person_link.test.sql` (44 new assertions) plus `people_rls.test.sql` updated 31 → 33 — **116/116 green**; `db lint --schema public --level error` clean; types regenerated (purely additive: `person_id` on Row/Insert/Update, `profiles_person_id_fkey`, two new functions); `pnpm turbo run typecheck lint` 8/8; `pnpm --filter @club/web build` green with placeholder env. Written up in `docs/migrations/P1.2-profiles-person-link.md`. Two findings recorded there: a bare `truncate public.people` now fails `0A000` (FK) before the SG-2 trigger, so the test asserts the `cascade` form too and still gets `P0001`; and `settings/actions.ts` already references `profiles.member_id`/`is_bar_staff`/`password_set` and a `members` table that do not exist in the baseline, and `book/actions.ts` upserts a `'booker'` role absent from the `user_role` enum — pre-existing, unrelated to P1.2. Rehearsed 2026-08-22 on preview branch `p1-2-profiles-people` seeded with prod's 39 profiles: both migrations applied, backfill 39/39 linked (all distinct, audit row written), pgTAP 116/116, lint clean; one P1.1 test made data-independent. PR #6, merged. Batched prod push of `20260822100000` + `20260822110000` done 2026-08-22: migration history 4/4 in sync, `db diff --linked -s public` empty, prod backfill linked all **39** profiles (38 at spec time, 39 by push time) to 39 people, one `migration.backfill` audit row written. Acceptance criteria met. |
| P1.3 | `guardianships` (guardian_person → child_person, relationship type) | Constraint: child must be minor; RLS: guardians see own links | ☑ 2026-08-22 (pushed to prod 2026-08-22) — `supabase/migrations/20260822120000_guardianships.sql` creates `public.guardianships` (`guardian_person_id`/`child_person_id` → `people(id)` both `on delete restrict`, `relationship` enum `public.guardian_relationship` ('parent','step_parent','grandparent','foster_carer','legal_guardian','other'), `notes`, `ended_at` soft end, `created_at`/`updated_at`/`created_by`, `check (guardian <> child)`, `check (ended_at >= created_at)`), plus `public.current_person_id()` — the `auth.uid() → profiles.person_id` STABLE SECURITY DEFINER helper every policy from here on uses (NULL for anon, EXECUTE revoked from `public` and `anon` by name per the P1.1 lesson). **SG-4 in full**: `guardianships_guard()` fires `before insert or update of guardian_person_id, child_person_id` (creation and retarget only — SG-4 says the age tests apply "at creation only" and the link outlives the minority, so a whole-row guard would freeze the row after the child's 18th birthday; deviation from the task's "BEFORE INSERT OR UPDATE" settled by SAFEGUARDING §1), enforcing child-is-minor via `is_minor()` (NULL dob accepted — SG-0 makes unknown a minor), guardian-is-adult-with-known-dob (`dob is not null and not is_minor_dob(dob)` — SG-4's "deliberate asymmetry"), and no self-guardianship; all four raise `P0001` with distinct messages, and the tests assert the messages. **SG-1.8 scaffolded exactly as SG-1's "Implemented by" directs**: `guardianships_conversation_guard()` on `before delete or update of guardian_person_id, child_person_id, ended_at` — body is `to_regclass('public.conversations') is null → return` today, and raises if `conversations` exists without the body having been replaced (fail-closed forcing function for P5.2, which replaces the BODY only). `ended_at` added to SG-1.8's column list as a strengthening (ending a link is indistinguishable from deleting it for SG-1). **SG-2 applied asymmetrically and deliberately**: `deny_truncate()` + `revoke truncate` yes; `deny_hard_delete()` **no**, because SG-1.8 requires the delete to stay reachable ("a flat reject makes a mistyped or genuinely-ended guardianship permanently unremovable, which is itself a data-protection problem") — so DELETE is granted to `authenticated`/`service_role` with a committee `FOR DELETE` policy, and the trigger, not the grant, is what will refuse the dangerous case. RLS: `guardianships_guardian_read` (own links, live and ended) + committee read/insert/update/delete; no anon, no staff, and **no child-read policy** (deferred to D5, documented). New `people_guardian_read` on `public.people`: a guardian reads a live-linked child's row **while `is_minor()`** — the D5 default that access lapses at 18, enforced in the policy not by mutating the row. Unique index is **partial** on live links (`where ended_at is null`) rather than SG-4's literal full `UNIQUE`, so an ended arrangement can be honestly re-established — flagged for Adam under SAFEGUARDING §6.2. Tests: `supabase/tests/guardianships.test.sql` (80 new assertions) + `people_rls.test.sql` policy set updated — **196/196 green**; `db lint --local --schema public --level error` clean; types regenerated (purely additive, +68/−0: table, enum in `Enums` and `Constants`, `current_person_id`); `pnpm turbo run typecheck lint` 8/8; `pnpm --filter @club/web build` green with placeholder env. Written up in `docs/migrations/P1.3-guardianships.md`. Known open gap recorded there and in DECISIONS.md: SG-7 requires committee reads of this table to be audit-logged, which needs P4.3's SECURITY DEFINER accessor + `write_audit()` pattern. Rehearsed 2026-08-22 on preview branch `p1-3-guardianships` (prod-shaped): migration applied, pgTAP 196/196, lint clean. PR #7 merged. **Pushed to prod 2026-08-22**: migration history 5/5 in sync, `supabase db diff --linked -s public` empty. Acceptance criteria met. |
| P1.4 | `roles` / person_roles: `club_admin`, `safeguarding_lead`, `coach`, `staff`, `member`, `parent`, `hirer` — replaces any ad-hoc role flags | RLS helper functions (`has_role()`) used by all subsequent policies | ☑ 2026-08-22 (pushed to prod 2026-08-22) — `supabase/migrations/20260822130000_roles.sql` creates enum `public.app_role` (the seven roles SAFEGUARDING §1.3 tabulates; a **separate** type from the baseline `user_role`, which is untouched) and `public.person_roles` (`person_id` → `people(id)` `on delete restrict`, `role`, `granted_at`, `granted_by` → `auth.users` `set null`, **`revoked_at` soft revoke**, `notes`, `updated_at`; partial unique `(person_id, role) where revoked_at is null`, same reasoning as P1.3's live-links index; `set_updated_at`). Helpers, all STABLE / SECURITY DEFINER / `search_path = public`, EXECUTE revoked from `public` **and `anon` by name** and granted to `authenticated` + `service_role`: `has_role(app_role)`, `has_any_role(app_role[])`, `person_has_role(uuid, app_role)` (the SG-6 hook — P2.1's composition-side guard must ask about *another* person), `is_club_admin()`, `is_safeguarding_lead()`. SECURITY DEFINER is load-bearing, not stylistic: `person_roles`' own insert policy calls `is_club_admin()`, so a SECURITY INVOKER helper would re-enter the table's RLS to evaluate itself. **Mapping** (one shared IMMUTABLE function, so the backfill and the trigger cannot drift): super_user/committee → `club_admin`; bar_manager/bar/staff → `staff`; member → `member`. **Nobody** gets `safeguarding_lead` (D9 — deriving it from `committee` would hand every committee member the Welfare Officer's readership of every concern, including concerns about themselves, which SG-3 excludes in terms), `coach` (no teams until P2.1), `parent` (§1.3: authority is the guardianship *link*, never the role) or `hirer`. `backfill_person_roles()` returning `(roles_created)` is idempotent and writes **one** `migration.backfill`/`person_roles` audit row carrying the mapping, not 39 — the per-grant SG-7 trigger is suppressed during it by a transaction-local GUC (`app.person_roles_audit_suppressed`, `is_local => true`, read in exactly one place), because 39 rows attributed to nobody would bury the grants a human actually made. SG-7: `person_roles_audit()` AFTER INSERT OR UPDATE writes `role.granted` / `role.revoked` (actor from `auth.uid()`, email from `auth.users`, `detail` = `{person_id, role}` and deliberately **not** `notes` — `audit_read` is `is_committee()` and wider than this table's own policies); a cosmetic `notes` edit writes nothing. **SG-2 applied in full here, unlike `guardianships`**: `deny_hard_delete()` + `deny_truncate()` + revoked DELETE/TRUNCATE (service_role named) + no FOR DELETE policy. Not inconsistent with P1.3 — SG-1.8 needed *that* delete reachable ("a flat reject makes a mistyped guardianship permanently unremovable"), whereas a mis-granted role is corrected by revoking it, and the grant is what authorises the SG-3 access SG-7 logs, so deleting it makes the audit row unexplainable. **Legacy bridge**: `trg_profiles_sync_person_roles` AFTER INSERT OR UPDATE **OF role** on `profiles` grants the newly mapped role and revokes the previously mapped one — **one-directional** (`profiles.role` is one value, a person holds several; `app_role` has values `user_role` cannot express; writing back would make the old app's RLS respond to new-model grants). Reverse is P1.6/Phase 4, which deletes this trigger. `handle_new_user()` deliberately **unchanged**: an AFTER INSERT trigger on `profiles` fires for a trigger-issued insert like any other — asserted, not assumed. `is_committee()`/`is_staff()`/`is_bar_manager()` **unchanged in behaviour** (three tests assert they still read `profiles`), so the ~38 baseline policies and all of apps/web are unaffected; policies migrate to `has_role()` table by table. The P1.1/P1.3 `TODO P1.4` policies are re-expressed: `people_committee_*` → `people_admin_read` (both admin roles) / `_insert` / `_update` (**club_admin only** — §1.3 gives safeguarding_lead a safeguarding write remit, not member administration), and `guardianships_committee_*` → `guardianships_admin_*` with **read AND write for both** admin roles, because SG-4 says so in terms and §1 makes the document outrank the task description; confirmed with Adam. Additive in effect: every committee/super_user profile holds `club_admin` as of the backfill in the same transaction, so nobody loses access — asserted as a regression, and the corollary (the rewrite is only safe *because* the backfill runs with it) is on the rehearsal checklist. RLS on `person_roles`: self-read of **live** roles only, admin read of everything (club_admin + safeguarding_lead), insert/update club_admin only, no anon/staff/coach/hirer. Tests: `supabase/tests/roles.test.sql` (**115 new assertions**) plus policy-list updates in `people_rls.test.sql` / `guardianships.test.sql` and entity-scoped audit assertions in `profiles_person_link.test.sql` — **311/311 green**, every count data-independent via `set_config(..., true)`. `db lint --local --schema public --level error` clean; types regenerated (purely additive, +89/−0: table, `app_role` in `Enums` and `Constants`, seven functions; header + `__InternalSupabase` preserved); `pnpm turbo run typecheck lint` 8/8; `pnpm --filter @club/web build` green with placeholder env. Written up in `docs/migrations/P1.4-roles.md`. Rehearsed 2026-08-22 on preview branch `p1-4-roles` seeded with prod's 39 profiles (4 committee/1 super_user/1 bar_manager/33 member): migration applied, pgTAP 311/311, lint clean, backfill mapping verified. PR #9, merged. **Pushed to prod 2026-08-22**: migration history 6/6 in sync, `supabase db diff --linked -s public` empty, backfill granted 5 `club_admin` / 1 `staff` / 33 `member` (39 profiles), one `migration.backfill` audit row, nobody granted `safeguarding_lead` (D9). Acceptance criteria met. |
| P1.5 | `resources` (type: `function_room`, `pitch`, extensible) and unified `bookings` + generalised `booking_payments` | New booking API path works for a test pitch resource; conflict-check function has tests | ☑ 2026-08-23 (pushed to prod 2026-08-23) — `supabase/migrations/20260823100000_resources_bookings.sql` creates enums `resource_type` (`function_room`,`pitch`), `booking_status` (the five prod values), `payment_status` (prod's three), `booking_kind` (`hire`,`block`,`fixture`,`maintenance`); tables `public.resources` (function-room pricing/extras as typed nullable columns, `resources text[]`→`amenities`, default buffers, `legacy_function_room_id`), `public.bookings` (half-open `[starts_at, ends_at)` timestamptz, `pre/post_buffer_minutes`, trigger-maintained `[blocked_from, blocked_until)` because `timestamptz ± interval` is STABLE, every non-derivable `room_bookings` column under its own name, `booker_person_id`→`people` + `booker_profile_id`→`profiles`, `legacy_room_booking_id`) and `public.payments` (generalised `booking_payments`, `legacy_booking_payment_id`, `stripe_payment_intent_id` for P4.1); GiST `bookings_no_overlap` carried over from the baseline pattern (`resource_id WITH =`, buffered `tstzrange … [)` `WITH &&`, pending/confirmed only); `booking_conflicts()` / `booking_has_conflict()` with the same filter and `p_exclude_booking_id`. RLS via `has_any_role(array['staff','club_admin'])` read/insert/update, `is_club_admin()` delete, booker self-read, anon reads active resources only; all non-public policies `TO authenticated` (load-bearing — see DECISIONS.md). Legacy tables untouched; no data copied. Tests: `supabase/tests/resources_bookings.test.sql` (**85 assertions** on a test pitch: overlap, containment, touching edges, overnight, cancelled/enquiry/quoted ignored, promoting an overlapping quote refused, pre/post buffers, function≡constraint, exclude-self, unspoofable blocked window, full RLS matrix) — **561/561 green**; `db lint` clean; types regenerated (+473/−0); `pnpm turbo run typecheck lint` 8/8; `@club/web build` green. Written up in `docs/migrations/P1.5-resources-bookings.md`. Rehearsed 2026-08-23 on preview branch `p1-5-resources` (prod-shaped, 7/7): push applied, pgTAP 561/561, lint clean; branch deleted. PR #11 merged (`38d64df`). **Pushed to prod 2026-08-23**: migration history 8/8 in sync, `supabase db diff --linked -s public` empty; the three tables are empty until P1.6 populates them. Acceptance criteria met. |
| P1.6 | Migrate `function_rooms`/`room_bookings`/`booking_payments`/`booking_emails` data into the unified structure; keep legacy tables as read-only views or renamed `_legacy` | Row counts reconcile; web app reads/writes unified tables; audit_log records migration | ☑ 2026-08-23 — **Complete.** Live at `https://portal.aomsportsclub.co.uk` on Vercel project `club-management-web` (monorepo `apps/web`); smoke test on the live hostname passed (public pages from `resources`/`bookings`, auth redirects, cron 200, no runtime errors). Legacy tables are `*_legacy` behind read-only views (PR #32); decommission no earlier than 2026-09-22. Old project `aomsc-function-room` still answers `roombooking.…` read-only until Adam adds that hostname to the new project as a redirect; old repo to archive then. History below. ◑ 2026-08-23 — **DB half done, rehearsed with real data.** `supabase/migrations/20260823110000_migrate_room_data.sql`: (1) SG-2 on `audit_log` (`deny_hard_delete` + `deny_truncate`, DELETE/TRUNCATE revoked incl. `service_role` — the SAFEGUARDING §4 Phase-1 audit item) and `public.write_audit(action, entity, entity_id, detail)`; (2) `public.booking_comms` (generalised `booking_emails`, `channel`, RLS staff/club_admin + booker self-read, `TO authenticated`); (3) **idempotent** `public.migrate_room_bookings()` (service_role only) upserting by `legacy_*_id` into `resources`/`bookings`/`payments`/`booking_comms`, removing unified copies of legacy hard-deletes, refusing unmapped status values, one `migration.backfill` audit row per run; (4) `public.reconcile_room_bookings()` — 20 `(check, legacy, unified, ok)` rows (counts, `sum(amount_pence)`, `sum(total_pence)`, status ×5, payment_status ×3, kind, payment/comm linkage, profile→person, Europe/London round-trip of every date/time, overnight); (5) runs the sync and **raises if any check fails**. Conversions: `(date + time) at time zone 'Europe/London'`, overnight +1 day, `booking_type 'block'`→`kind`, `booker_person_id` from the profile (no `people` row for walk-in hirers — DECISIONS.md). **The legacy tables stay live**: the public site still deploys from the old repo (P0.4 deferral) and Claude holds no Vercel credentials, so the rename-to-`_legacy` + read-only views step is `docs/runbooks/P1.6-cutover.md`, to be run with the Vercel repoint under a write freeze (re-sync → reconcile → rename → repoint → smoke test; abort paths written). Tests: `supabase/tests/migrate_room_data.test.sql` (62) — **623/623** across 9 files; lint clean; types +81/−0. **Rehearsed 2026-08-23 twice with prod's real rows** (39 users/profiles, 1 room, 40 bookings, 9 payments, 32 emails, seeded from a prod snapshot): locally and on preview branch `p1-6-migrate` (push applied, then the cutover re-run scenario `migrate_room_bookings()` → reconciliation **20/20 ok** — 40/40 bookings, 30 person-linked, 7 overnight, £901.00 of payments — pgTAP 623/623 with the real rows present, lint clean; branch deleted, snapshot files deleted). Written up in `docs/migrations/P1.6-migrate-room-data.md`. **App half done**: apps/web booking data layer on `resources`/`bookings`/`payments` with `@club/db` types; `src/lib/booking-time.ts` (Europe/London ⇄ instant, DST-correct, 35 vitest cases); `booking_has_conflict` RPC + SQLSTATE 23P01 handled on every conflicting write, recurrence series pre-checked before writing; dead `events` query removed; `noUncheckedIndexedAccess` back on; out-of-scope P0.4 drift (`members`/`login_history`/`push_subscriptions` tables, `booker` role) isolated behind `createLegacyAdminClient()` with a documented list for Phase 4. PR #12 merged (`e323dbf`), CI green. **Pushed to prod 2026-08-23**: history 9/9, `db diff` empty, `reconcile_room_bookings()` **20/20 ok** on prod, audit row `{resources 1, bookings 40, payments 9, comms 32, removed 0}`. **Vercel project created 2026-08-23** via the Vercel connector: `club-management-web` (`prj_N9U2qCQOwiuqI4u06Kd7qnyBQrBA`, team `theheathens1878s-projects`), linked to `TheHeathens1878/Club-Management-System` `main`, root directory `apps/web`; first production build green (turbo built `@club/web`, cron from `vercel.json` registered) at `club-management-web-theheathens1878s-projects.vercel.app`. It 500s at runtime only because no env vars are set ("Your project's URL and Key are required") — the connector cannot write env vars. Env vars set by Adam 2026-08-23 (secrets regenerated); production deploy smoke-tested (/book from `resources`, cron 200, no runtime errors). **Cutover executed 2026-08-23 12:47 UTC:** `20260824100000_legacy_room_tables_to_views.sql` (PR #32, CI green, rehearsed on branch `p1-6-cutover`, pgTAP 1298/1298) pushed to prod — final sync + reconcile gate + rename in one transaction; old names are now read-only views, `*_legacy` tables closed to app roles; reconcile 20/20 on prod after the rename; `migration.cutover` audit row. Old hostname still serves read-only through the views. **REMAINING — two dashboard actions for Adam:** (a) Vercel → `club-management-web` → Domains → add **`portal.aomsportsclub.co.uk`** (Adam's choice 2026-08-23; CNAME → `cname.vercel-dns.com`) plus `roombooking.aomsportsclub.co.uk` as a redirect to it; (b) set `NEXT_PUBLIC_SITE_URL=https://portal.aomsportsclub.co.uk` on `club-management-web` and redeploy. Then Claude smoke-tests on the live hostname and ticks P1.6; `*_legacy` decommission after 30 clean days (~2026-09-22). Original note — write-freeze the old site, re-run `migrate_room_bookings()` + reconcile, apply the rename-to-`_legacy` + views migration, repoint the domain, smoke-test. Until then the legacy tables stay live and the unified copy is re-synced at cutover. The acceptance criterion "web app reads/writes unified tables" is met in code and on CI; "row counts reconcile" and "audit_log records migration" are met on prod. |
| P1.7 | `guardian_consents` (per child, per purpose, granted by an active guardian; revocable; audited), `consent_type` enum, safeguarding settings in `site_settings` (`min_account_age` 13, `unsupervised_messaging_min_age` 14, admin-editable, DB-validated), SG-10 account-eligibility trigger on `profiles`, `handle_new_user()` invite flow | Violation tests for SG-10; under-age signup refused; invited eligible minor succeeds; settings validation; audit rows | ☑ 2026-08-22 (pushed to prod 2026-08-23) — `supabase/migrations/20260822140000_consents_settings.sql` implements SAFEGUARDING **SG-10 in full** plus the §1.5 derived terms **SG-0.1 / SG-0.2** that SG-1.9 and SG-9 are stated in. Creates enum `public.consent_type` (`app_account`, `unsupervised_messaging`; P2.2 adds the SG-5 photo values by `alter type … add value`) and `public.guardian_consents` (`child_person_id`/`guardian_person_id` → `people(id)` `on delete restrict`, `consent_type`, `granted_at`, `granted_by`/`revoked_by` → `auth.users` `set null`, **`revoked_at` soft revoke**, `expires_at` (present, honoured, always NULL pending **D12**), **`notice_version` NOT NULL** (SG-9: "so what they were told is evidenceable later"), `notes`, `updated_at`; partial unique `(child_person_id, consent_type) where revoked_at is null` — SG-10 in terms; `set_updated_at`). Helpers, all STABLE / SECURITY DEFINER / `search_path = public`, EXECUTE revoked from `public` **and `anon` by name** (each takes somebody else's person id and would otherwise be a probe): `safeguarding_setting_int(text)` (fail-closed to 13/14 for an absent **or** non-integer row; an unknown key raises rather than inventing a default), `has_active_consent(uuid, consent_type)`, `is_account_eligible(uuid)` (SG-0.1), `is_supervision_exempt(uuid)` (SG-0.2), plus internal `is_at_least_age(date,int)` expressed as `is_minor_dob()`'s exact complement so the two can never disagree about a birthday. **Settings**: `safeguarding.min_account_age` = 13 and `safeguarding.unsupervised_messaging_min_age` = 14 seeded `on conflict do nothing`, with three `site_settings` triggers all scoped to `safeguarding.%` keys — validation (plain integer, no padding; floor 13 per **D11**; `min ≤ unsup < 18` evaluated against the other key's *current* value so **neither editing order** can pass an invalid pair), deletion refused, and `settings.changed` (SG-7) on UPDATE only, with `old`/`new` as JSON numbers. The imported app is untouched: none of its 27 keys matches `safeguarding.%`, and the two baseline `site_settings` policies (dashboard-made, spaces in the names) are left byte-identical. **Grant guard** (BEFORE INSERT, SECURITY DEFINER — unlike P1.3's, because a *parent* writes this table and RLS-scoped reads would make the guard answer differently per caller): child is a minor; guardian is an adult with a KNOWN dob (SG-4's asymmetry, re-checked because a dob correction could have changed the answer); an **ACTIVE** `guardianships` link (`ended_at is null`) — the link, never the `parent` role; and `granted_by`, when supplied, is the guardian **or a `club_admin`** (the paper-form case — `safeguarding_lead` excluded, P1.4's reasoning). **Change guard** (BEFORE UPDATE): child/guardian/type/`granted_at`/`granted_by`/`notice_version` immutable for everyone (a consent is evidence, not a form); no un-revoking in place (a re-grant is a new row, which is what the partial index is for); a caller with no admin role — including `service_role` and the owner — may change only `revoked_at`/`revoked_by`. **SG-2 EXTENDED to this table** exactly as SG-10 requires ("extends SG-2's named list of four tables… recorded as a strengthening under §6.2"): `deny_hard_delete()` + `deny_truncate()` + revoked DELETE/TRUNCATE (service_role named) + no FOR DELETE policy. **SG-7**: `guardian_consents_audit()` writes `safeguarding.consent.granted` / `.revoked` with `detail` = `{child_person_id, guardian_person_id, consent_type, notice_version}` and deliberately **never `notes`**; a row inserted already revoked is history, not an event; P1.4's transaction-local suppression GUC pattern is present (`app.guardian_consents_audit_suppressed`) but **unset by this migration** — it exists for the Phase 3 import, and both halves are tested. **SG-10 on `profiles`**: `trg_profiles_sg10_account_eligibility` BEFORE INSERT **and BEFORE UPDATE OF person_id** (the second arm is a §6.2 strengthening — repointing a profile reaches the same prohibited state with nothing happening in `people` or `guardian_consents`), refusing a **known** minor who is not account-eligible and naming which limb failed. Its name is chosen to sort **after** `trg_profiles_ensure_person`, which is load-bearing and asserted, not assumed. The **single** `people_dob_guard()` gains SG-10's re-check as its third invariant (§4: "not several triggers on the same column") and becomes SECURITY DEFINER, because its new `EXISTS` on `profiles` would otherwise be silently false for a `club_admin` who is not also `profiles.role = 'committee'` — a silently-skipped safeguarding check being the worst available outcome; its default EXECUTE grants are withdrawn to match. `handle_new_user()` gains the **invite flow**: `raw_user_meta_data->>'person_id'` is adopted only when it parses as a uuid, names an existing person with **no profile** and an **active `app_account` consent**; every other case creates a new person exactly as P1.2 wrote it, and it still **never matches on email**. RLS: guardian read/insert/update keyed on a **live** link, child self-read, `club_admin` + `safeguarding_lead` read all **and revoke** (SG-10 names both roles and §1 makes the document outrank the task description), no admin INSERT policy, nothing for anon/staff/coach/hirer. Tests: `supabase/tests/consents_settings.test.sql` (**165 new assertions**) — **476/476 green** across 7 files, every count data-independent via `set_config(..., true)`; **no existing test file needed changing** (P1.7 adds one table and alters no other table's policy or trigger list). `db lint --local --schema public --level error` clean; types regenerated (purely additive, **+76/−0**: table, `consent_type` in `Enums` and `Constants`, five functions; header + `__InternalSupabase` preserved); `pnpm turbo run typecheck lint` 8/8 (needs `--concurrency=1` on this machine — eight parallel `tsc`/`eslint` processes abort V8 with exit 134); `pnpm --filter @club/web build` green with placeholder env. Written up in `docs/migrations/P1.7-consents-settings.md`. **AWAITING ADAM** (SAFEGUARDING §6.1 names the first two): the unknown-DOB carve-out, without which every adult self-signup would be refused; the SG-2 extension to `guardian_consents`; plus the `safeguarding_lead`-may-revoke and `granted_by`-may-be-a-club_admin readings recorded in DECISIONS.md. C9's citation row is still `[Adam to verify]`, so per §2.3 the *mechanism* may ship but sign-off may not. Rehearsed 2026-08-23 on preview branch `p1-7-consents` (prod-shaped, history 6/6): migration applied, pgTAP 476/476, lint clean. PR #10 merged (`c7b56b3`). **Pushed to prod 2026-08-23**: migration history 7/7 in sync. The four AWAITING ADAM items above remain open as sign-off questions (mechanism shipped per §2.3). |

---

## 6. Phase 2 — Teams, seasons, fixtures

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P2.1 | `teams`, `seasons`, `team_memberships` (person, team, season, role: player/coach/manager) | RLS: coaches see own teams; admins see all | ☑ 2026-08-23 (pushed to prod 2026-08-23) — `supabase/migrations/20260823120000_teams_seasons.sql`: `seasons` (one `is_current`), `teams`, enum `team_role` (player/coach/assistant_coach/manager), `child_facing_roles` lookup (§4: not a hard-coded list; undesignated fails closed; rows never deleted; lead may flip), `team_memberships` (soft `left_at`, partial unique on live rows), plus the minimum **`certifications`** / **`certification_exemptions`** SG-6 needs (brought forward from P4.3 — DECISIONS.md): exemptions `safeguarding_lead`-only (must be the caller), reason mandatory, ≤30 days by CHECK, immutable, revoke-only; SG-2 extended to both evidence tables. **SG-6 tier 1 in full** via ONE shared evaluation `is_child_facing_compliant()` (verified in-date fa_dbs AND safeguarding_children, or an active exemption for that team): (a) staff side and (b) composition side in `team_memberships_sg6_guard()` (insert, retarget, promote; errors name the non-compliant people; exemption use audited), (c) dob side added to the **single** `people_dob_guard()` at P1.7's marker. SG-7: `safeguarding.certification.change` and `.exemption` (granted/used/revoked). RLS: club_admin+lead read all, club_admin writes, team staff read own team (`is_team_staff()`), self-read, guardian reads minor child; coaches cannot read others' certifications. Tests: `supabase/tests/teams_seasons.test.sql` (**73**) — every SG-6 tier-1 test SAFEGUARDING.md names, as owner / club_admin / service_role — **696/696** across 10 files; lint clean; types +307/−0; turbo 10/10. Written up in `docs/migrations/P2.1-teams-seasons.md`. Rehearsed 2026-08-23 on preview branch `p2-1-teams` (prod-shaped 9/9): push applied, pgTAP 696/696, lint clean; deleted. PR #13 merged (`b08c34d`). **Pushed to prod 2026-08-23**: history 10/10, `db diff` empty. Acceptance criteria met. |
| P2.2 | `registrations` (person, season, status, forms/consents captured) | Registration flow spec'd; consent fields include photo consent per child | ☑ 2026-08-23 (pushed to prod 2026-08-23) — `supabase/migrations/20260823130000_registrations.sql`: `consent_type` gains the four SG-5 decisions (`photo_team_album`, `photo_club_website`, `photo_social_media`, `photo_press`) — **photo consent reuses `guardian_consents`** exactly as SAFEGUARDING §4's P1.7 block directs (one grant guard, one immutability rule, one SG-7 trigger, one `has_active_consent()` for P4.5; per-season scoping via `expires_at`; absence = refused) — DECISIONS.md; `registration_status` + `registrations` (person × season, optional team, sensitive `form jsonb` + `form_version`, submitted/decided stamps, partial unique on live rows); `registrations_guard()` (adult self / **active guardian** of a minor / club_admin / service_role; status machine; decisions club_admin only; subject or guardian may withdraw; identity immutable; **approval with a team creates the player membership so P2.1's SG-6 guard runs at approval time and a non-compliant team fails the approval naming the coach**); `registration.submitted`/`.decided` audit never carrying the form; RLS self/guardian/admin — **coaches read nothing** (medical data; pitch-side access is a Phase 4 audited accessor). Spec: `docs/specs/P2.2-registration-flow.md` (who registers whom, form v1 shape, the six consent checkboxes and their `expires_at`, decision flow, D1 open). Tests: `supabase/tests/registrations.test.sql` (**53**) incl. SG-5's `consent_granted_by_non_guardian_throws`, fail-closed absence, immediate withdrawal, per-season expiry — **749/749** across 11 files (P1.7's enum expectation updated as it anticipated); lint clean; types +91/−2 (the enum literal); turbo 8/8. Written up in `docs/migrations/P2.2-registrations.md`. Rehearsed 2026-08-23 on preview branch `p2-2-registrations` (prod-shaped 10/10): push applied, pgTAP 749/749, lint clean; deleted. PR #14 merged (`129742c`). **Pushed to prod 2026-08-23**: history 11/11, `db diff` empty. Acceptance criteria met (flow spec + photo consent per child). **Form builder + the SG-5 consent writes (2026-08-25, wave7).** `20260825140000_registration_builder.sql`: `registration_questions` (the form as data — system rows map to a rendered block; `photo_consents`/`gdpr_consent`/`terms` locked so a trigger refuses to archive them or make them optional), `set_registration_question_order()`, `people.photo_path` + `set_person_photo()`, `people.id_verified` + `set_id_verified()` + `needs_id_document()`, `identity_documents` (three-year `purge_after`, no update/delete grants, `identity_document_purged()` service-only) and two private 5MB buckets (`person-photos`, `identity-documents` — ID reads are club_admin only). Web: `/registrations/form` builder (drag + arrows), `/join` step 3 rendered from the table and finally WRITING the guardian_consents rows spec §3 tabulates, player photo as the club-wide avatar, ID upload mandatory until the tick. Edge Function `id-docs-purge` (cron `20 5 * * *`) written but NOT deployed. **Who may pick which team (2026-08-26).** Adam: "limit the teams they can choose to their own age group (be careful not to fall foul of UTC issues) and the age group above", "males cannot join female teams but females can join males", and "make the default proof of identity a passport". `20260825500000_registration_team_rules.sql`: `people.sex` (nullable `male`/`female` — the join form had asked for a biological sex and thrown it away outside a waiting-list entry), `fa_age_band(dob, on)` — the ONE age rule, season year (boundary 1 July) minus birth-cohort year (boundary 1 September), computed from Postgres `date` values, which carry no zone, with `fa_age_band_today()` taking today in **Europe/London**; `team_admits_sex()` (a girls’ team admits female players only, `teams.gender` already existed from `20260824170000`); `may_register_for_team()`; `set_person_sex()` and `registration_subjects()`. `registrations_guard()` gains two paragraphs: the SEX rule binds **everybody, club administrators included**, on INSERT and on any team change, and refuses only where both facts are on record; the AGE BAND rule binds non-administrators only — exactly matching the screen, where “show all teams” is a club administrator’s escape and nobody else’s. Web: `lib/waiting-list.ts` age helpers now take the yyyy-mm-dd STRING (`ageBandNumber`, `ageGroupFromDobString`, `eligibleAgeBands`, `teamOfferedToPlayer`, `londonToday`) and the Date-shaped `ageGroupFromDob` is gone — `new Date("2014-09-01")` is midnight UTC, which is 31 August west of Greenwich and the wrong side of the FA cohort line; one shared `<TeamChoiceFields/>` drives the picker on /join step 3, /family and /register-a-player; the ID-document `<select>` defaults to passport off `ID_DOCUMENT_KINDS[0]`. Tests: vitest `waiting-list` (**33**), pgTAP `registration_team_rules` (**44**). |
| P2.3 | Fixtures schema in main project: `fixtures` (home/away, opponent, competition, kickoff, status, `source` = fulltime/manual, `external_ref`, venue → nullable FK to pitch `resources`), `availability`, `selections`. Team settings store the team's FA Full-Time identifiers — **editable in-app by club admins**: a team settings screen where the admin pastes the team's Full-Time URL; the app parses out league/season/division/team IDs, test-fetches, and shows a preview of upcoming fixtures for confirmation before saving. Changing the URL (e.g. new season, league change) re-links without orphaning existing fixtures; a bad URL shows a clear validation error. RLS: club_admin only | Admin can add, update, and remove a team's Full-Time link entirely in-app with fixture preview; no config files or DB edits involved | ☑ 2026-08-23 (pushed to prod 2026-08-23) — `supabase/migrations/20260823140000_fixtures.sql`: `team_fulltime_links` (PK team; `source_url` must be fulltime.thefa.com; `league_id`/`ft_season_id`/`division_id`/`fixture_group_key`/`ft_team_id`; `enabled`; last import status incl. `challenge`; **re-link = UPDATE, fixtures keyed by `(team_id, external_ref)` are never touched — tested**); `fixtures` (team × season, opponent text, `is_home`, competition, `kickoff_at`, status scheduled/postponed/cancelled/played/abandoned, source fulltime/manual, `external_ref` required+immutable for fulltime rows, `venue_resource_id` → a pitch only, paired scores, `last_seen_at`); `availability` and `selections` with membership guards (a coach cannot be selected; selection carries no SG-6 weight); helpers `fixture_team_id()`, `is_team_member()`, `is_team_guardian()`, `can_act_for()`. RLS: fixtures readable by any logged-in person, written by club_admin + team staff; availability self/guardian/staff; selections staff-written, team-read; links club_admin only. Verified live 2026-08-23: Full-Time URL scheme (`league`, `selectedSeason`, `selectedDivision`, `selectedFixtureGroupKey`, `selectedTeam`, `displayFixture.html?id=` as the stable ref) and that **Cloudflare challenges after a few requests** — parser classifies `challenge`, importer backs off, admin screen says so. Tests: `supabase/tests/fixtures.test.sql` (**46**) — **795/795** across 12 files; lint clean; types +275/−0. Rehearsed 2026-08-23 on preview branch `p2-3-fixtures` (fresh: full 12-migration replay): pgTAP 795/795, lint clean; deleted. Written up in `docs/migrations/P2.3-fixtures.md`. **`packages/fulltime`** (`@club/fulltime`, no runtime deps, Deno-compatible): `parseFullTimeUrl`/`buildFixturesUrl`, `parseFixturesPage` (class-based row detection; header mapping only when safe), `fixturesForTeam`, `stableExternalRef` (displayFixture id, else FNV-1a hash), `parseCsvFixtures`, `classifyResponse` (`challenge` on the Cloudflare interstitial), `fetchFullTimePage` + `RateLimiter` — **92 vitest cases** against four recorded real pages + one synthetic table. **apps/web**: `/teams` (teams + seasons admin) and `/teams/[id]` with the Full-Time link panel — paste any Full-Time URL → parse (clear error for non-Full-Time/league-less URLs) → server-side fetch → preview table (London times, home/away, opponent, competition, status, team-name chips when no match, seasons, parser warnings) → save/update/remove/enable; a Cloudflare challenge is surfaced in words and the link can still be saved; parser never reaches the client bundle; committee-gated; audited. turbo 13/13, build green. PR #15 merged. **Pushed to prod 2026-08-23**: history 12/12, `db diff` empty. Acceptance criteria met (add/update/remove a link entirely in-app with fixture preview). |
| P2.4 | **FA Full-Time importer**: scheduled Edge Function fetches each mapped team's fixtures from fulltime.thefa.com and upserts by `external_ref` (stable hash of teams+competition+date if no ID). Handles reschedules/postponements as updates, never duplicates; imports results after matches. Parser isolated in `packages/fulltime` with recorded-HTML fixture tests; import failures alert admin. Manual fallback: paste a Full-Time URL or CSV to import on demand | Importer runs nightly against Adam's real team pages; reconciles cleanly on repeat runs; parser tests pass against saved page snapshots; fallback works with importer disabled | ☑ 2026-08-23 (pushed to prod 2026-08-23) — `supabase/migrations/20260823150000_fulltime_import.sql`: `fixture_import_runs` (every attempt, outcome, counts, warnings); **`import_fixtures(team, season, fixtures jsonb, trigger, url, warnings)`** — the ONE place an import means something: upsert by `(team_id, external_ref)`, reschedules/postponements/results are UPDATEs of the same row (never duplicates), unchanged refs get `last_seen_at`, absence is not cancellation, manual rows never touched, unmapped status refused; `record_fixture_import_failure()` (run row + link status + `fixtures.import_failed` audit = the admin alert); `fulltime_import_targets()`; **pg_cron → pg_net → `invoke_edge_function(name)`** nightly 03:15 UTC reading `project_url`/`service_role_key` from **Vault** (no-op with a NOTICE until the two secrets exist). Edge Function `supabase/functions/fulltime-import` (Deno; imports the parser from `_shared/fulltime`, a verbatim copy of `packages/fulltime/src` kept in sync by `scripts/check-shared-sync.sh` in CI): service-role or club_admin JWT, per-team fetch → classify → parse → `fixturesForTeam` → `import_fixtures`, `RateLimiter` between teams, challenge/error → `record_fixture_import_failure`. Tests: `supabase/tests/fulltime_import.test.sql` (**30**: repeat run reconciles cleanly, no duplicates, reschedule/postpone/result as updates, absence ≠ cancellation, manual untouched, validation, failure audit, RLS). Written up in `docs/migrations/P2.4-fulltime-importer.md`. **Manual fallback** on `/teams/[id]`: paste-URL and CSV tabs → preview (team-name chips when nothing matches) → `import_fixtures` with trigger `manual_url`/`manual_csv`; last 10 `fixture_import_runs`; "Run now" posts to the Edge Function (clear message while undeployed). Rehearsed 2026-08-23 on preview branch `p2-4-5-importer` (fresh replay of 15 migrations; pgTAP 861/861; lint clean; cron job registered). turbo 13/13 (51 web tests), build green. PR #16 merged; **pushed to prod 2026-08-23** (history 14/14, `db diff` empty); `fulltime-import` **deployed** (PR #17: Deno needs `.ts`-suffixed imports); Vault secrets `project_url`/`service_role_key` set so the 03:15 UTC job is live. Acceptance: "reconciles cleanly on repeat runs" + "parser tests pass against saved snapshots" + "fallback works with importer disabled" met; **"runs nightly against Adam's real team pages" needs Adam to save his teams' Full-Time links on `/teams` (and Cloudflare may still challenge — the run records `challenge` and the manual import is the fallback).** **Widget import (2026-08-24, PR #43 + follow-up):** the import source is now each team's Full-Time **widget** (`/js/cs1.html?cs=<lrcode>` from the "add to your website" snippet — fixtures **and results**, team-scoped, same `displayFixture.html?id=` keys so it reconciles onto existing rows). Cloudflare fingerprints the TLS client, not the IP: Deno `fetch()` is 403 for every Full-Time URL, **pg_net (libcurl) is 200** for both widget and page when sent with a browser UA + `Accept-Language` — so `20260824190000_fulltime_pgnet_fetch.sql` adds `fulltime_http_get`/`fulltime_http_result` (club_admin or service role; Full-Time URLs only; only its own responses readable) and the Edge Function, the team-page preview and the manual URL import all fetch through them (`fetchViaPgNet`). `team_fulltime_links.widget_code` (widget-only links need no league/season ids); `fulltime_import_targets()` returns it; trigger `manual_widget`. Team page: paste the snippet → preview (team name read off the widget; venue + score columns) → save → **Import now**. Tests: `supabase/tests/fulltime_widget.test.sql` (22), `packages/fulltime` 103 vitest. Runbook rewritten (no club PC needed; local runner kept as fallback). **Nightly path fixed** (`20260824193000_fulltime_prefetch.sql`): pg_net serves its queue serially, so a fetch queued by a function that pg_net is itself waiting on deadlocks — cron now prefetches at 03:12 (`fulltime_prefetch()`) and the 03:15 function reads the responses (`fulltime_prefetched`). `fulltime-import` uses the shared `requireServiceRole`. **Verified live 2026-08-23: U14 Mavericks linked by widget code, 20 fixtures imported (run 2).** **Club-wide widgets** (`20260824205000`): Full-Time's club fixtures + club results codes in `site_settings` (Teams → Club Full-Time widgets card, preview shows matched teams) feed every active team without a per-team link — name-suffix matching (`matchClubTeam`), parser generalised to the 5-cell club rows, "Postponed" group headings and single-cell scores; prefetch re-keyed per URL. **Name matching hardened (`20260824220000`)** after a wrongly pasted widget imported AFC Urmston's season onto U14 Pythons: matching anchors on `site_settings.fulltime_club_name` (default ft_team_name prefix too), folds U08/U8 + Girls/squad qualifiers (girls' league club widget = second code, multi-code settings), a wrong-team widget records a failure instead of importing, and `notify_club_admins()` posts one in-app run summary to every club admin (the oversight channel). |
| P2.5 | **Pitch allocation**: club admin allocates a home fixture to a pitch — this creates a linked `booking` on that pitch resource (kickoff + configurable pre/post buffer), running the same conflict-check as all bookings. Admin dashboard: unallocated home fixtures list, weekend pitch grid view, drag/reassign. Reschedule from Full-Time moves the linked booking and flags conflicts to admin rather than silently double-booking | Allocation blocks a conflicting hire booking and vice versa; reschedule test moves booking; unallocated-fixtures view accurate | ☑ 2026-08-23 (pushed to prod 2026-08-23) — `supabase/migrations/20260823160000_pitch_allocation.sql`: `fixtures.booking_id`/`duration_minutes`/`allocation_conflict`, `bookings.fixture_id`; **`allocate_fixture(fixture, pitch, pre?, post?)`** (club_admin; home+scheduled only; pitch must be active non-room; creates or MOVES the linked `kind=fixture` booking through the ordinary path so `bookings_no_overlap` is the arbiter; conflicts named — `Hirer 16/09 11:00–13:00 (hire)` — as 23P01 and nothing written); `unallocate_fixture()`; **reschedule trigger** `fixtures_sync_booking` moves the booking, or on a clash sets `allocation_conflict` + `fixtures.allocation_conflict` audit row and leaves the booking where it was (flagged, never double-booked); postponed/cancelled frees the pitch, back to scheduled re-books; a linked fixture booking cannot be cancelled/moved/deleted directly (`bookings_fixture_guard`); `unallocated_home_fixtures` view + `pitch_grid(from, to)`. Tests: `supabase/tests/pitch_allocation.test.sql` (**36**) — **allocation blocks a conflicting hire and vice versa; reschedule moves the booking; clash flags; unallocated view accurate** (all three acceptance criteria). **861/861** across 14 files; lint clean; types +221/−1; turbo 10/10. Written up in `docs/migrations/P2.5-pitch-allocation.md`. **Dashboard** `/pitches`: unallocated home fixtures with an Allocate control (pitch select, buffers defaulting to the pitch's, 23P01 message shown verbatim), weekend grid (Sat/Sun × pitches × 30-min London slots, buffers shaded, click a fixture → select-based "Move to…"/Unallocate), flagged fixtures with the audit's "Clashed with …" text; team page shows pitch + clash badge. `src/lib/pitch-grid.ts` (16 vitest cases incl. DST). Rehearsed with P2.4 (above). PR #16 merged; **pushed to prod 2026-08-23**. Acceptance criteria met. |

---

## 7. Phase 3 — Neon migration & cutover

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P3.1 | Audit Neon schema **and discover the auth mechanism** (per Q1: check for Clerk SDK/tables, NextAuth tables, local password hashes). Document every table, row counts, auth findings, and mapping to target schema in `MIGRATION_MAP.md` | Auth mechanism identified with evidence; Adam has reviewed and approved the mapping | ◑ 2026-08-23 — Q1 answered **from the source code** (repo `TheHeathens1878/aom-fc-pitch-booking` cloned to `C:/Projects/aom-fc-pitch-booking`, commit `6881cca`; earlier live-site evidence in DECISIONS.md agrees): NextAuth Credentials + JWT, no adapter tables, `bcryptjs` cost-12 hashes (`$2a$12$`) → Strategy A import; locked `@placeholder.invalid` child accounts get no login. Full Prisma schema audited (62 models: `User` with optional DOB, `UserContact` guardianship, teams, pitch bookings, training, team chat, coach groups, lobby, waiting list, attendance; **no payments/Stripe/fixtures**). `MIGRATION_MAP.md` rewritten: §3 data model, §4 per-table mapping onto `people`/`auth.users`/`person_roles`/`guardianships`/`teams`/`team_memberships`/`resources`/`bookings`/`conversations`/`messages` + new `waiting_list_*` tables, with decisions D-P3-1…4; §5 lists the data checks. Read-only URL supplied later on 2026-08-23; §5 filled from data: 61 users (all bcrypt `$2a$`), 73 teams, 15 pitches, 76 bookings (no overlaps), 73 waiting-list entries, negligible chat; **all 56 adults have NULL DOB**; 6 emails already in prod auth. Chat/attendance/medical decisions moot. **REMAINING — Adam:** approve §4 and D-P3-2 (coach certifications), D-P3-5 (6 email matches), D-P3-6 (adult DOB carve-out). |
| P3.2 | Auth mapping plan based on P3.1 findings: legacy users → `auth.users` + `people`. If Clerk: export users, create Supabase users, send password-reset comms. If custom hashes: attempt hash import via Supabase Auth admin API, else password reset | Dry-run creates users on a branch DB; comms email drafted for Adam | ◑ 2026-08-23 — **Built and dry-run on preview branch `p3-neon`:** `apps/web/scripts/neon-auth-import.mjs` (Strategy A, bcrypt hash via `auth.admin.createUser`, `user_metadata.person_id`) created **57/57** linked accounts on the branch (61 users − 3 placeholder − 1 known minor with an email, who needs the P1.7 consent flow); `handle_new_user()` adopts an adult person only when person_id **and** email match. First-login DOB gate shipped (`needs_dob_completion()`, `complete_own_dob()`; web middleware → `/complete-profile`, mobile `(app)` layout → `complete-profile`); walked one imported coach through it on the branch. Runbook `docs/runbooks/P3.2-auth-mapping.md` rewritten with the comms email. REMAINING: Adam approves the comms email; PR #27 review. |
| P3.3 | Import: `pg_dump` Neon → restore into `neon_legacy` schema; write transformation SQL (legacy users/bookings/payments → unified tables) | Full rehearsal passes on a Supabase branch; reconciliation script (row counts, sums of payment amounts) passes | ◑ 2026-08-23 — **Rehearsal passed on preview branch `p3-neon`** (deleted after): `scripts/neon-restore.sh` restored the live Neon dump (61 users, 73 teams, 15 pitches, 77 bookings, 73 waiting-list entries); `migrate_neon()` → 61 people, 57 roles, 73 teams, 15 pitches, **102 bookings** (77 + 6 training + 19 closure×pitch), 74 waiting-list rows, 71 queued (4 guardianships + 67 memberships; 4 applied at once — known-DOB players), season 2026/27 created; `reconcile_neon()` **27/27 ok**; re-run idempotent. One legacy venue closure (18–26 Jul) clashed with live bookings on two pitches → imported `cancelled` with an "OVERLAP AT IMPORT" note for Adam to review (closures tolerated; booking/training downgrades would fail reconcile). pgTAP `neon_import.test.sql` 116 tests, full suite 1266, `db lint` clean. Migration `20260824000000_neon_import.sql` on PR #27 — **not yet on prod** (human review: people/guardianships/memberships/auth). |
| P3.4 | Cutover **as soon as P3.3 passes** (per Q5): schedule a quiet-evening write-freeze, freeze Neon writes, run migration, repoint pitch app + Stripe webhooks, smoke test. Go/no-go checklist and abort procedure written before the window | Checklist executed; pitch bookings working against Supabase; Neon set read-only | ☑ 2026-08-23 — **Complete.** Steps 5–7 done after Adam moved `coaches.`/`membership.aomsportsclub.co.uk` to `club-management-web` (both 200 on the new app; `/recruitment` → `/waiting-list` 308); move-notice email **57 sent / 0 failed** through `comms-dispatch` (Microsoft Graph, PR #38) — which also surfaced and fixed the scheduled-invocation 401 (PR #39). Legacy Vercel project `aom-fc-pitch-booking` stays paused as the abort lever; Neon read-only 30 days → P3.5 (no earlier than 2026-09-22). Open: a real coach sign-in walk-through (Adam). ◑ 2026-08-23 — **Window executed 2026-08-23 ~17:30 UTC (steps 1–4 + gap fix).** Pre-load at 16:20 UTC unfrozen (idempotent): `neon-restore.sh` → prod `neon_legacy` (User 61, Team 73, Booking 77, WaitingListEntry 73), `migrate_neon()` 55+6 people / 53 roles / 73 teams / 15 pitches / 102 bookings / 74 WL rows / 71 queued (4 applied), `reconcile_neon()` **27/27**, `neon-auth-import.mjs` **51 created, 0 failed** (57 imported logins incl. 6 pre-existing). **Gap found and fixed before repoint:** apps/web had no waiting-list pages — PR #36 added public `/waiting-list` (anon RPC), `/recruitment` 308, staff desk `/waiting-list/manage`; live on portal. **Freeze:** legacy Vercel project `aom-fc-pitch-booking` paused (503) — Neon owner URL not needed; final dump identical, migrate delta 0, reconcile 27/27, auth 0/0. **REMAINING:** step 5 — Adam adds `coaches.aomsportsclub.co.uk` + `membership.aomsportsclub.co.uk` to Vercel project `club-management-web` (moves them off the paused project); step 6 smoke test on those hostnames + a real coach sign-in (Claude cannot); step 7 comms needs the Azure/Graph secrets (`AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `MAIL_FROM`) set on Supabase Edge Functions (draft in P3.2 runbook); step 8 Neon stays read-only 30 days → P3.5. Earlier: ◑ 2026-08-23 — `docs/runbooks/P3.4-cutover-checklist.md` rewritten against the built import (no Stripe/payments in Neon; 8-step window; abort list by legacy key). Pre-window items for Adam: appoint a `safeguarding_lead` (prod has none — no imported coach can be activated without one), P1.6 Vercel cutover, owner Neon URL for the write freeze, DNS TTL. REMAINING: merge PR #27, push to prod, schedule the window. |
| P3.5 | Decommission: after 30 days clean, archive final Neon dump to cold storage, delete Neon project, drop `neon_legacy` | Sign-off from Adam recorded in DECISIONS.md | ☐ |
| P3.6 | **Post-cutover parity (added 2026-08-23).** The gap analysis after P3.4 found the coach/admin pitch tooling had not been built. Adam: "fix the gaps the admin and coach will hit in week one, in ranking order; no more emails to coaches whilst we fix the app". Ranked gaps: (1) team membership editor; (2) people/contact admin; (3) pitch booking + training creation; (4) self-registration + approvals, with first-login role tiles (Player/Parent/Coach/Admin) shaping the menu; (5) in-app notifications; (6) full pitch calendar, separate from the function-room calendar; (7) venue/pitch admin; (8) availability + attendance; (9) family self-service; (10) waiting-list extras + public recruitment page | Each gap live on portal; pgTAP + CI green; no coach email sent | ☑ 2026-08-23 — **All ten gaps live on portal.aomsportsclub.co.uk** (PRs #42–#56 incl. schema `20260824110000`…`20260824180000`; Full-Time widget import via pg_net by the parallel session, `190000`/`193000`). Routes: /people, /teams/[id] members + recruiting + pitch bookings, /pitches/book|mine|requests|calendar|manage|[id], /register, /welcome (role tiles), /approvals, /notifications (+bell), /family, /registrations, /recruitment (public), /waiting-list/manage access|export|priorities. pgTAP 1393/1393 before the Full-Time migrations; CI green on every PR; no runtime errors after deploy. **Still no coach emails** (Adam's rule) — lift explicitly. Follow-ups noted by builders: guardian photo-consent UI (SG-5) not built; parents cannot edit child contact details (by policy); second-adult invite deferred (no email). Smoke on live hostname 19:35 UTC: public pages 200, signed-in routes gate to login. **Attendance markers completed (Adam, 2026-08-24):** every fixture — home, away, allocated or not — now has its own marker page `/teams/[id]/fixtures/[fixtureId]` (household three-way answer + staff squad panel, writes `availability`), and headcount chips (✓/✗/?) sit on the team page's fixtures tab, overview, bookings tab and the `/pitches/[bookingId]` sheet ("N going of squad", players only — staff answers never inflate the count). Mobile: the fixtures tab gains a **Training** section (pitch_calendar → `booking_availability` toggle per household player, same control as fixtures). Training creation was already live at /pitches/book (weekly series). Web `lib/headcount.ts` + mobile `lib/sessions.ts` are unit-tested. **Coach bookings are requests, not confirmations (Adam, 2026-08-25):** `20260825170000_coach_pitch_approval.sql` makes `bookings_team_guard()` PIN a non-administrator's pitch booking to `pending` on INSERT (it previously left a posted `confirmed` to be refused by `bookings_team_staff_insert` with a bare 42501, losing the request) and adds `pitch_request_notify()` — an AFTER INSERT statement trigger that tells every live `club_admin` in-app through `public.notify()`, one message per statement, no email. Confirming stays `club_admin`/`staff`-only via the existing guard and `bookings_staff_update`. `supabase/tests/pitch_requests_approval.test.sql` (24). **The hat, not the role — and a coach may book a match (Adam, 2026-08-25):** the Save-as control was still offered on ROLE, and a committee sign-in maps to `club_admin`, so wearing the Coach hat still produced "Pitch booking confirmed." `20260825400000_coach_match_booking.sql` adds `request_team_pitch_booking()` — the TEAM path made explicit, `security invoker` (so RLS and `bookings_team_guard()` still decide whether the row may exist) with **no status parameter at all**, so a booking made through it is `pending` whatever role the caller holds. `/pitches/book` and `createPitchBooking` both resolve the hat (`resolveRoleView(getStoredRoleView(), capabilities)`): Club admin hat + `is_club_admin()` → the direct insert and the Save-as control; any other hat → the request path, and the success message says "requested", not "confirmed". The staff/club_admin exemption in the guard is DELIBERATELY untouched (20260825170000's header: the function-room desk's own closures). Match (`booking_kind = 'fixture'`) joins Training and Other use — `bookings_team_staff_insert` widened by that one enum value, and the guard now fences `fixture_id` off the coach path entirely (a coach may not set one on INSERT, nor touch a row that already has one, so `allocate_fixture()` keeps the league games). The form asks whether the opposition is internal (team picker) or external (free text) and pre-fills the label "U14 Mavericks v Sale Sharks" without ever overwriting one typed by hand (`nextSuggestedLabel`). Cancel gained a confirm step on `/pitches/mine` and the team's Bookings tab, and `cancelPitchBooking` now treats zero rows changed as the refusal it is. `supabase/tests/coach_match_booking.test.sql` (22), vitest `pitch-booking` (10). **An internal match is one game on two teams' pages (Adam, 2026-08-26):** 20260825400000 stored the opposition only in `bookings.occasion`, a sentence, so a club-v-club match appeared on the booking team's page alone. `20260825410000_internal_match_fixtures.sql` adds `bookings.opponent_team_id` (set by `request_team_pitch_booking()`, which gains `p_opponent_team_id`; CHECK-constrained to matches and never the team itself) and `fixtures.mirror_fixture_id`, a self-reference because a match has exactly two sides forever. `create_internal_match_fixtures(booking)` — SECURITY DEFINER, club_admin only, idempotent under `FOR UPDATE` — is called by `confirmPitchBooking` the moment an administrator confirms the request (never at request time: a pending slot must not put a game on the opposition's matchday tab) and writes two rows in the current season (`seasons.is_current`, the rule every other lookup uses): HOME on the booking's own team, holding `booking_id` exactly as `allocate_fixture()` would, and the AWAY mirror on the opposition with `booking_id` null — both mirrors cannot hold it, because `fixtures.booking_id` and `bookings.fixture_id` are both UNIQUE. Cancelling either fixture cancels the other (`fixtures_cancel_mirror()`), and cancelling the booking cancels both (`bookings_cancel_internal_match()`, with `bookings_fixture_guard()` widened by exactly that one case — a league fixture's slot is still unallocated on /pitches, because that game exists whether or not a pitch does). Status only, never a delete. External matches are untouched: no team row, no fixture, as before. `supabase/tests/internal_match_fixtures.test.sql` (35). **Individual or family is counted in PLAYERS (Adam, 2026-08-26):** `create_membership()` decided the kind by `array_length(person_ids, 1) > 1` — the number of PEOPLE on the submission — so a parent registering one child was filed and charged as a family. `20260825520000_membership_kind_by_players.sql` puts the rule in one place, `membership_kind_for(membership_id)`: a person counts as a player for the membership's SEASON when they hold a live `team_memberships` row with `role = 'player'`, or a `registrations` row with `status in ('pending', 'approved')` — the pair `registrations_live_idx` already calls live, and pending counts on purpose so a family is a family from the moment it asks rather than from the moment the queue clears. Two or more players is a family; nought or one is an individual. Nine statement-level triggers with transition tables (three each on `membership_people`, `registrations` and `team_memberships`, because Postgres will not attach transition tables to a multi-event trigger) re-derive every affected membership, so the second child registered six weeks later flips the kind and a withdrawal flips it back; `refresh_membership_kind()` writes only where the answer changed and `memberships` carries no trigger of its own, which is what makes the set non-recursive. The migration backfills every existing row through the same helper. `person_memberships` — a security_invoker view over `membership_people`, `memberships` and `seasons` — carries the tag per person and widens nothing: the badge appears on `/people` (in the Type · Membership cell), in the CSV export, on the `/people/[id]` header, and in a **Club membership** card there that names the other people on the membership and links straight through to their records. pgTAP `membership_kind.test.sql` (17), vitest `membership-kind` (11); `join_flow.test.sql`'s people-count assertion restated as the player rule. |

---

## 8. Phase 4 — CRM modules

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P4.1 | Subs & payments: Stripe products/subscriptions per season/team pricing; webhook Edge Function; `payments` ledger | Test-mode subscription lifecycle covered by tests; arrears view per team for coaches | ◑ 2026-08-23 — `supabase/migrations/20260823170000_subscriptions.sql`: `subscription_plans` (season, optional team, one_off/monthly/annual, Stripe ids), `subscriptions` (player × plan; **payer must be an adult with known DOB who is the player or an active guardian** — guard; pending→active→past_due/cancelled/completed; payer creates pending and may cancel own pending; status otherwise club_admin/service_role), **`payments` becomes the ledger** (`booking_id` nullable, `subscription_id`, `kind` derived, Stripe ids, refunds, at most one link, unique intent), `stripe_events` (insert-first idempotency, service_role only), `subscription_arrears` view (net of refunds) via a new `display_name()` helper that reveals a name only to self/guardians/team staff/admins. Edge Functions `stripe-webhook` (signature-verified; checkout.session.completed, invoice.paid, invoice.payment_failed, customer.subscription.deleted, charge.refunded) and `stripe-checkout` (payer-only; creates product/price on the fly). Tests: `subscriptions.test.sql` (**43**) incl. the test-mode lifecycle as service_role. Rehearsed 2026-08-23 on preview branch `p4-crm` (4 migrations; pgTAP 1031/1031; lint clean). PR #19 merged; **pushed to prod 2026-08-23**; functions deployed. UI: `/subs` (committee: plans, arrears, record payment, cancel), `/my-subs` (household, Pay now → `stripe-checkout`, degrades when Stripe is not configured). **REMAINING — Adam:** `supabase secrets set STRIPE_SECRET_KEY STRIPE_WEBHOOK_SECRET`, register `/functions/v1/stripe-webhook` in Stripe (test mode first); one-off plans rely on Checkout `invoice_creation` (function README). |
| P4.2 | Arrears comms: scheduled Edge Function → Resend reminders with escalation tiers; all sends logged | Dry-run mode; opt-out honoured; audit_log entries | ◑ 2026-08-23 — Edge Function `arrears-reminders` (weekly Tue 09:00 UTC via `invoke_edge_function`): tiers at ≥14/≥30/≥60 days (`arrears.tier*_days` settings), one send per tier per subscription guarded by `outbound_messages` (entity `subscriptions`, template `arrears_tierN`), delivered through P4.4's `enqueue_message` so opt-out (reminder category), suppression and the platform `comms.dry_run` all apply and every send is logged. Scheduled (`20260823250000`, Tue 09:00 UTC), deployed; runs are platform dry-run until `comms.dry_run` is `false` and a provider key exists. REMAINING — Adam: provider secrets (the app emails via Microsoft Graph today; `comms-dispatch` expects `RESEND_API_KEY` for queued email — pick one and set it). |
| P4.3 | Safeguarding: `certifications` (DBS, first aid, coaching badges; expiry dates), expiry-nudge scheduler (90/30/7 days), `safeguarding_concerns` (restricted RLS per §2.4) | Access tests prove only safeguarding_lead/admin can read concerns; nudges fire in test | ◑ 2026-08-23 — `supabase/migrations/20260823180000_safeguarding_concerns.sql`: **SG-3 as written** — `safeguarding_concerns` + `_notes` with ALL privileges revoked from anon/authenticated/**service_role** (and the ref sequence), SECURITY DEFINER accessors that audit BEFORE returning even on zero rows (`report_concern`, `my_concern_receipts` (D3 receipt), `read_concerns` (lead + club_admin, never the subject even as admin; unauthorised → zero rows AND an audit row), `update_concern`/`add_concern_note`/`read_concern_notes` (lead only)), SG-2 guards, narrative immutable, FORCE RLS as layer 4, an `audit_log` guard refusing concern content in `detail` (SG-7). SG-8: `people.legal_hold` lead-only + audited; pseudonymising anyone in an open concern or under hold refused. **SG-6 tier 2**: `certification_nudges` (90/30/7, never re-sent), `due_certification_nudges()`, `person_compliance_status()`, `compliance_report()` (nightly "non-compliant and still assigned"); `audit_read` re-expressed on `person_roles` with `safeguarding.%`/`messaging.%` narrowed to lead + club_admin. Edge Function `safeguarding-nudges` (daily 06:30 UTC) sends nudges to the person + leads, the compliance report and the SG-1 nightly list. Tests: `safeguarding_concerns.test.sql` (**60**) — every SG-3 test by name incl. `direct_select_on_concerns_throws_for_service_role`, `service_role_read_via_accessor_writes_audit_row`, `audit_row_written_even_when_read_returns_zero_rows`, `expiry_nudge_fires_at_90_30_7`, `expired_coach_appears_in_daily_compliance_report`. PR #19 merged; **pushed to prod**; scheduled 06:30 UTC; deployed; UI under P5.6. Acceptance met: access tests prove only lead/admin read concerns; nudges fire in test. |
| P4.4 | Comms preferences & audit: per-person channel preferences (email/SMS/push/in-app), suppression list | Every outbound message routed through one internal API that checks preferences | ◑ 2026-08-23 — `supabase/migrations/20260823190000_comms.sql`: `comms_preferences` (absence = enabled; **SMS opt-in**), `comms_suppressions` (hard block, lower-cased), `outbound_messages` (THE log), **`enqueue_message()`** — the one internal API (address resolution, suppression → preference (skipped for transactional) → `comms.dry_run` setting → row with the decision), `mark_message_sent/failed` (return counts), `queued_messages()`; Edge Function `comms-dispatch` (every 5 min; Resend/Twilio/Expo push via `push_tokens`). Tests: `comms.test.sql` (**33**). PR #19 merged; **pushed to prod**; `/settings/comms` (own + children's preferences, committee suppression list); **every outbound email in apps/web now goes through `enqueue_message` → send (Microsoft Graph) → `mark_message_sent/failed`**, with suppression/preference/dry-run decisions honoured (the acceptance criterion). REMAINING — Adam: provider secrets for the dispatcher. |
| P4.5 | Media on **Supabase Storage** (per Q4; image transforms + signed URLs, no Cloudinary): albums per team/event, per-child photo-consent enforcement — children without consent excluded from bulk downloads and public galleries at query level | Test: unconsented child's photos never appear in bulk export; signed URLs expire | ◑ 2026-08-23 — `supabase/migrations/20260823200000_media.sql`: private `media` bucket; `media_albums` (visibility → required consent purpose), `media_items`, `media_subjects`; **`authenticated` has no SELECT on items/subjects** — reads are `media_gallery()`/`media_export()` filtered by `media_item_showable()` (confirmed subjects only — **untagged fails closed** — every minor subject consented for the purpose); `media_export` writes `media.bulk_export {item_count, excluded_unconsented}` before returning; withdrawal flags every item for quarantine and the `media-quarantine` Edge Function moves the object so old signatures break; `media-signed-url` mints ≤15-min URLs only for what the functions return; SG-8 legal hold/redaction. Tests: `media.test.sql` (**34**) — the SG-5 named tests. PR #19 merged; **pushed to prod**; scheduled every 10 min; deployed; UI: `/media` albums, `/media/[id]` gallery from `media_gallery()` with server-minted ≤15-min signed URLs only for returned paths, upload + `confirm_media_subjects` tagging ("needs tagging — hidden" badge), `/media/[id]/export` JSON. Acceptance: unconsented child never in export (pgTAP); signed URLs expire (TTL capped in `lib/media.ts`). |

---

## 9. Phase 5 — Messaging (WhatsApp-style)

Design against `SAFEGUARDING.md` first. P5.1 is a written spec, human-reviewed before code.

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P5.1 | Messaging spec against **Cheshire FA safeguarding guidance** (per Q6): conversation types (`dm`, `group`, `team`, `announcement`), participant rules, retention, moderation & export. Encode adult/minor rules as testable invariants, each citing the guidance it implements; Adam verifies citations against current published Cheshire FA / FA Safeguarding Children Policy | Adam approves spec | ◑ 2026-08-23 — `docs/specs/P5.1-messaging-spec.md`: conversation types (dm/group/team/announcement) and who creates/joins/posts; participants (`basis`, oversight rows never exist — SG-1.5); the SG-1 rule as one function at six trigger sites; messages (soft delete, no edits, redaction); SG-9 oversight and the banner; realtime/receipts/typing/push (no body in push for conversations with a minor); retention (D7 placeholders, job disabled); moderation/export. Each rule names its SG invariant. **AWAITING ADAM: approval + §2.3 citations (C1, C4, C6, C7, C8), D2, D5, D7.** Built on the 2026-08-22 decisions per SAFEGUARDING §2.3 (mechanism ships, sign-off waits) — DECISIONS.md. |
| P5.2 | Schema: `conversations`, `conversation_participants` (with `last_read_message_id`, joined/left), `messages` (reply_to, soft-delete), attachments in Supabase Storage; implements SG-1.9 and SG-9 | RLS: participants only; DB-level enforcement of §2.4 invariants with tests that attempt violations and fail | ◑ 2026-08-23 — `supabase/migrations/20260823210000_messaging.sql`: `conversations` (`supervised_by_lead`, `legal_hold`, `closed_at`), `conversation_participants` (`basis`, `left_at`, `last_read_message_id`, `muted_until`; oversight rows impossible by CHECK), `messages` (soft delete, immutable body/sender, redaction), `message_attachments` + private `attachments` bucket; **SG-1 in one function `conversation_is_compliant()` with hypothetical parameters**, called from participants (per-row AFTER constraint trigger, which also SETS `supervised_by_lead` for SG-1.9), messages (SG-1.7), the single `people_dob_guard()` (SG-1.2), `guardianships_conversation_guard()` (P1.3's scaffold body replaced — SG-1.8 incl. `ended_at`), `guardian_consents` revocation and a raised `unsupervised_messaging_min_age` (SG-1.9, reject-unless-closed), and `sg1_nightly_check()`; `supervised_by_lead` cannot be cleared while a minor is active; **SG-9** `read_conversation_as_lead`/`export_conversation_as_lead`/`redact_message_as_lead` (lead or club_admin, mandatory reason, minor-involved only, audit before returning even when empty, never a participant row; no admin read policy exists); SG-2 on all three tables incl. service_role; participant-only RLS. Tests: `messaging.test.sql` (**75**) — every SG-1/SG-1.9/SG-9 test by name as owner/club_admin/service_role. Two NULL-comparison defects caught by the tests before rehearsal (DECISIONS.md). Rehearsed on preview branch `p5-messaging` (5 migrations; pgTAP 1148/1148; lint clean). REMAINING: PR; prod push; P5.4 UI. |
| P5.3 | Team conversations auto-membership: triggers sync participants from `team_memberships` + guardians of minor players | Adding a player adds their guardians; leaving team marks participant left (history retained) | ◑ 2026-08-23 — `20260823220000_team_conversations.sql`: `team` + `announcement` rooms per team/season created on demand; membership synced from `team_memberships` (staff/member) and, for minors, every active guardian (**guardians inserted first** — the SG-1 check is per row); leaving marks `left_at` (history kept); guardians leave when no live minor child of theirs remains; new/ended guardianships sync; backfilled. Tests: `team_conversations.test.sql` (**23**) — adding a player adds their guardians; leaving marks left; rejoin; guardian of two players once; ending one of two guardianships. Rehearsed on `p5-messaging`. REMAINING: PR; prod push. |
| P5.4 | Realtime delivery in web app: subscribe, send, read receipts, typing indicator (broadcast) | Two-browser manual test passes; unread counts correct | ◑ 2026-08-23 — `/messages` (list with preview + unread via `last_read_message_id`, "Lead can read" badge), `/messages/[id]` (Realtime `postgres_changes` on `messages` by conversation, `typing:<id>` broadcast, read receipts via a server action, soft-delete own, report with reason → `report_message`, leave with the DB's SG-1.1 refusal verbatim, **persistent SG-9 banner on `supervised_by_lead` conversations**, read-only notice for announcement recipients), `/messages/new` (DM/group; SG-1 refusals verbatim, half-built conversations closed). User-scoped client throughout — RLS decides. REMAINING: the two-browser manual test (Adam) after the Vercel deploy; unread counts verified then. **WhatsApp-style thread (2026-08-23, `20260824270000`):** grouped bubbles with tails + day chips (Today/Yesterday, London), sent/read ticks off `last_read_message_id` over realtime participant updates, reply-with-quote (renders the long-dormant `reply_to_id`; quotes pass through `visibleBody()` so deleted/redacted bodies never leak), emoji reactions (`message_reactions`, participant RLS, none on announcements/closed/removed messages, un-react is a hard delete — DECISIONS.md), photo attachments on web (user-scoped storage client, `attachments/<conv>/<msg>/…`, signed URLs), Enter-to-send + auto-grow composer, optimistic sending keyed by client-supplied ids, unread divider, load-earlier paging. pgTAP messaging_reactions (16); presence/last-seen deliberately deferred (minor-disclosure decision). **@mentions (2026-08-25, `20260825330000`):** typing `@` opens a picker of the conversation's LIVE participants (name/word-prefix match, arrows + Enter on the desk, 44px rows on a phone, Escape closes; Enter still cannot send — it only chooses); `sendMessage` re-resolves the typed names SERVER-side against the live participant set and writes `message_mentions` through the SECURITY DEFINER `mention_people(uuid, uuid[])` (sender-only, live-participant-only, readable by whoever may read the message, no client write policy at all), then notifies each mentioned person once via `notify()` on the admin client — in-app, never the sender, no email. Mentions render as bold tinted chips (not links: a route the reader may not hold is not leaked). vitest `mentions` (35), pgTAP messaging_mentions (18). |
| P5.5 | Push fan-out: DB webhook → Edge Function → Expo push API for offline participants; respects preferences (P4.4) | Push received on test devices; no push for muted conversations | ◑ 2026-08-23 — `20260823240000_push_tokens.sql` (`push_tokens`, self-managed, service_role reads/prunes) and Edge Function `push-fanout` (Database Webhook on `messages` INSERT → `enqueue_message(push)` per active unmuted participant except the sender; **body omitted when the conversation involves a minor**) delivered by `comms-dispatch` via the Expo push API (P4.4). REMAINING: PR; prod push; create the Database Webhook in the dashboard (→ `/functions/v1/push-fanout` with `WEBHOOK_SECRET` header); deploy; device test (P6.3). |
| P5.6 | Safeguarding tooling: conversation export for safeguarding_lead, report-message flow, retention job | Export produces complete history incl. soft-deleted messages, access-logged in audit_log | ◑ 2026-08-23 — `20260823230000_messaging_tooling.sql`: `report_message()` → a `safeguarding_concerns` row through SG-3's `report_concern()` with a structured `[message:… conversation:…]` prefix + audit; retention — `retention.messages_months` (24) and `retention.enabled` (**false**, D7), `message_retention_candidates()`, `retention_run(dry_run)` forced to dry-run while disabled, skips legal holds and open concerns, redacts-not-deletes, never touches `audit_log`; `20260823250000_schedules.sql` registers comms-dispatch (5 min), media-quarantine (10 min), safeguarding-nudges (06:30), arrears-reminders (Tue 09:00), retention dry-run (Mon 04:00). Export completeness incl. soft-deleted is tested. Tests: `messaging_tooling.test.sql` (**19**). PR #20 merged; **pushed to prod 2026-08-23**. UI: `/safeguarding` (lead or committee: `read_concerns`, `compliance_report`, SG-9 oversight form), `/safeguarding/concerns/[ref]` (notes, lead-only triage/notes), `/safeguarding/report` (anyone; `my_concern_receipts`), `/safeguarding/conversation` read-only via `read_conversation_as_lead` + `/export` JSON via `export_conversation_as_lead` (incl. soft-deleted; each access audit-logged — the acceptance criterion). Retention stays dry-run (D7). **Super-user purge (2026-08-25, `20260825380000`):** Adam asked for a real delete for GDPR erasure and test accounts, so SG-2 gains its first exception, written up as SAFEGUARDING.md SG-2.1 — `is_super_user()`, `purge_message(uuid, text)` and `purge_person(uuid, text)`, all SECURITY DEFINER and super-user-only (42501), gated on an `audit_log` row that must exist in the SAME transaction before `deny_hard_delete()` will let anything go, scoped to nine tables that exclude `audit_log`, `safeguarding_concerns`, `safeguarding_concern_notes` and `media_items`. Refuses every legal hold, every message or person a concern names, the caller themselves, and the two references that would destroy somebody else's record. The triggers are not disabled and the DELETE/TRUNCATE revokes stand. UI: a super-user-only "Delete permanently" panel on `/people/[id]` (type the name, give a reason) and a "Delete permanently" item in the thread's message actions; storage objects are removed with the service-role client only after the RPC has returned. pgTAP `super_user_purge` (**50**). |

---

## 10. Phase 6 — Mobile app (Expo)

| Task | Description | Acceptance criteria | Status |
|---|---|---|---|
| P6.1 | Expo app scaffold in `apps/mobile`; Supabase auth (email + magic link), shared client from `packages/shared`; EAS build profiles | Dev build signs in on iOS + Android simulators | ◑ 2026-08-23 — `apps/mobile` rebuilt on Expo SDK 57 + Expo Router: `(auth)` sign-in (email+password) and magic-link (PKCE, `aomclub://auth/callback`, 6-digit OTP fallback) screens, `(app)` "My club" (person + live team memberships through the user client only — RLS decides) and Settings/sign-out; `packages/shared` gains `createReactNativeSupabaseClient()` + a structural `SupabaseAuthStorage`; auth storage is `expo-secure-store` with code-point chunking (sessions exceed the 2 KB limit) and AsyncStorage fallback/migration; token refresh driven from `AppState`; `app.config.ts` (bundle `uk.co.aomsportsclub.club` + `.development`/`.preview` variants, scheme `aomclub`), `eas.json` (development/preview/production), `.env.example`. 37 vitest cases (chunking, deep-link parsing, club mapping); turbo **14/14**; `expo-doctor` 20/21 (the duplicate-React finding comes from apps/web's React 19.0 pin — harmless for native). **REMAINING — Adam:** `eas login`/`eas init` (project id), per-profile `EXPO_PUBLIC_*` env vars, Android keystore + Apple dev credentials on first `eas build --profile development`, add `aomclub://auth/callback` to Supabase Auth redirect URLs, then the simulator sign-in check that is the acceptance criterion. |
| P6.2 | v1 screens: my teams, fixtures (auto-imported from Full-Time, with pitch shown once allocated) + availability toggle, subs status + pay (Stripe payment sheet), profile. Admin-role users additionally get the pitch allocation view and Full-Time link management per team (both can be web-views initially) | Coach and parent test personas complete core journeys incl. seeing next fixture with pitch and setting availability | ◑ 2026-08-23 — tabs My teams / Fixtures / Subs / Messages / Profile in `apps/mobile/app/(app)/(tabs)`; a household context (self + guarded children via `guardianships`, names via `display_name`); Fixtures show pitch name once allocated ("Pitch TBC" before) with a three-way availability control per fixture per household member (upsert `availability`; offered only where the DB would accept it); Subs from `subscription_arrears` with **Pay via Stripe web checkout** (`stripe-checkout` Edge Function + `expo-web-browser`; a native payment sheet is a later swap — deliberate deviation, DECISIONS.md); Profile with admin section opening the web `/pitches` and `/teams` (needs `EXPO_PUBLIC_WEB_URL`). Everything via the user client — RLS decides. 155 vitest cases across 11 files. **REMAINING — Adam:** EAS project + credentials, `EXPO_PUBLIC_WEB_URL`, then the coach/parent persona device test that is the acceptance criterion. |
| P6.3 | Messaging UI: conversation list, thread view, attachments, read receipts, push handling (`expo-notifications`) | Feature parity with web messaging for participants | ◑ 2026-08-23 — conversation list (preview, unread via `last_read_message_id`, supervised badge) and thread (`apps/mobile/app/(app)/messages/[id].tsx`): Realtime on `messages` (+20 s poll backstop), send, mark read, long-press soft-delete own / report (`report_message` with a mandatory reason), image attachments (`expo-image-picker` → `attachments` bucket → `message_attachments`, signed URL via the user client), **SG-9 banner** (non-dismissible, `accessibilityRole="alert"`, wording pinned by tests) on `supervised_by_lead` conversations; push: `expo-notifications` permission + Expo token upserted into `push_tokens`, tap → `aomclub://messages/<id>`. Two DB gaps it found are fixed in `20260823260000_messaging_followups.sql` (participant-scoped SELECT on the `attachments` bucket; `messages`/`conversation_participants` added to the Realtime publication; service_role may prune `push_tokens`). REMAINING: Database Webhook for `push-fanout` (dashboard), APNs key + FCM JSON via `eas credentials`, device test. |
| P6.4 | Store readiness: icons/splash, privacy policy (messaging + minors data — flag for Adam's review), App Store/Play data-safety forms, EAS Submit | TestFlight + internal Play track builds distributed to club testers | ◑ 2026-08-23 — `docs/mobile/store-readiness.md`: privacy-policy DRAFT (children's data, guardian consent, SG-9 oversight stated plainly, processors, retention placeholders), App Store privacy and Play data-safety answers, EAS Submit steps. **REMAINING — Adam:** review/publish the policy (flagged: messaging + minors), club crest assets, Apple/Google developer accounts, TestFlight + internal Play track. |
| P6.5 | Web parity check: bookings remain web-first; deep links from push into app | Documented in README | ☑ 2026-08-23 — README "Web vs mobile" table: bookings web-first; admin allocation/links web; teams/fixtures/subs/messaging native; deep links `aomclub://messages/<id>` and `aomclub://auth/callback`. |

---

## 11. Working agreements for Claude Code / Finn-loop

- One Linear issue per task above; branch naming `phase-N/task-id-slug`.
- `finn-spec` output for any task in Phases 3, 5, or touching §2.4 must be approved by Adam before `finn-build` runs.
- PR description must state: migrations included (y/n), RLS changes (y/n), data touched, rollback step.
- Rollback: every migration has a down path or a documented restore procedure; cutover tasks (P3.4) have a written go/no-go checklist and abort procedure.
- When blocked on an open question (§3), stop and ask — do not assume.
