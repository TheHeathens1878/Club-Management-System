import { Card, CardDescription, CardHeader, CardTitle } from "@club/web";

export const InCard = () => (
  <div style={{ maxWidth: 420 }}>
    <Card>
      <CardHeader>
        <CardTitle>Function room</CardTitle>
        <CardDescription>Seats 80, licensed bar, free parking. Enquiries answered within two days.</CardDescription>
      </CardHeader>
    </Card>
  </div>
);
