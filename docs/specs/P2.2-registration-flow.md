# P2.2 — Registration flow specification

Status: implemented in `20260823130000_registrations.sql` (schema + rules);
the web/mobile screens that drive it are P6.2 / a later web task. This
document is what those screens must do, and why.

## 1. Who registers whom

| Subject | Submitted by | DB rule |
|---|---|---|
| Adult (known DOB, 18+) | themself, or a `club_admin` on their behalf | `registrations_guard()` INSERT branch; policies `registrations_self_insert` / `registrations_admin_insert` |
| Minor (incl. **unknown DOB**, SG-0) | an **active guardian** (`guardianships.ended_at is null`), or a `club_admin` typing in a paper form | guard checks the link — never the `parent` role (SAFEGUARDING §1.3); policy `registrations_guardian_insert` |
| Anyone, via the Phase 3 import | `service_role` (no `auth.uid()`) | guard passes; `submitted_by` NULL is honest |

One live (pending/approved) registration per person per season
(`registrations_live_idx`). A rejected or withdrawn one is final; register
again with a new row.

## 2. What the form captures

`registrations.form jsonb` (`form_version` says which shape). Version 1:

```json
{
  "emergency_contact": { "name": "", "phone": "", "relationship": "" },
  "medical": { "conditions": "", "medication": "", "allergies": "" },
  "previous_club": "",
  "preferred_position": "",
  "kit_size": "",
  "terms_accepted_at": "2026-08-01T10:00:00Z",
  "terms_version": "2026-1"
}
```

`form` is **sensitive**. Read policy: the subject, their active guardians,
`club_admin`, `safeguarding_lead`. **Coaches cannot read it** — pitch-side
access to a medical note is a Phase 4 audited accessor (SG-7), not a table
read. `audit_log.detail` never carries the form (tested).

## 3. Consents for a child — captured at the same moment

For a minor, the registration screen shows the guardian **four separate**
photo decisions (D4, SG-5) and, where relevant, the two account decisions
from P1.7. Each "yes" is one `guardian_consents` row; each "no" is **no row**
(absence = refused, fail closed):

| Checkbox | `consent_type` | `expires_at` |
|---|---|---|
| Team album (private, team only) | `photo_team_album` | season `ends_on` 23:59 Europe/London |
| Club website / public gallery | `photo_club_website` | season end |
| Club social media | `photo_social_media` | season end |
| Press / local media | `photo_press` | season end |
| App account for the child (P1.7, SG-10) | `app_account` | NULL (until D12) |
| Unsupervised 1:1 messaging with adults, 14+ (P1.7, SG-1.9) | `unsupervised_messaging` | NULL (until D12) |

Rules the DB already enforces (P1.7 guard, reused unchanged): the granter
must be an adult with a known DOB holding an **active** guardianship to the
child; `notice_version` is mandatory (the screen passes the id of the consent
wording shown, e.g. `photo-2026-1`); a consent is immutable — withdrawal is
`revoked_at`, and re-consent is a new row. Every grant/withdrawal writes
`safeguarding.consent.granted` / `.revoked` (SG-7).

Per-season expiry is why re-registration re-asks every year: a consent given
two seasons ago is not evidence of a current decision (D12 reasoning).

Adults' own photo preferences are outside SG-5 and are captured in
`form.photo_preferences` (informational).

**Version 3 (2026-08-25).** `emergency_contact` left the form. Emergency
contacts (up to two) are rows in `public.emergency_contacts` against the
PERSON (`20260825150000`), edited on My Profile, on Children (with "I am the
first emergency contact" for the guardian) and on the admin person page, and
written only through `set_emergency_contacts()`. `submitTeamRegistration`
refuses a registration for a person with none on record. Rows written under
versions 1–2 keep the contact inside `form`; the queue shows it only when the
person's record has none.

**The contact record's read-only copy (`20260825230000`).** Adam, 2026-08-25:
"the registration form should update read-only information in the contact
record (consents, health etc). This is overwritten on each registration."
`public.person_registration_details` is one row per person —
`(person_id pk, registration_id, season_id, details jsonb, updated_at)` — written
ONLY by the AFTER INSERT trigger `registration_details_snapshot()`, which
upserts `medical`, `kit_size`, `previous_club`, `preferred_position`,
`photo_preferences` and `custom` out of `form` (a whitelist; the terms and GDPR
stamps stay on the registration, being evidence about one submission rather
than a fact about the person). It is deliberately NOT on `people`, which team
staff and the committee can read: the snapshot carries the three `registrations`
read policies word for word — subject, active guardians, `club_admin`,
`safeguarding_lead` — and has no write policy at all. Shown read-only on
/people/[id] ("From the latest registration") and, collapsed, under each child
on /family, where the four live SG-5 photo consents from `guardian_consents` are
listed beside it. Existing registrations were seeded once by the migration.

**Photo permissions start ticked (2026-08-25).** The four SG-5 boxes (and an
adult's own four preference boxes) render `defaultChecked`. That is the
STARTING POSITION only: an unticked box still writes no `guardian_consents` row
and absence of a row is still refusal, so SG-5 remains fail-closed.

**A name against the ID tick (2026-08-25).** `set_id_verified()` has always
stamped `people.id_verified_by` with `auth.uid()`; the registrations queue and
the person page now print who it was, resolved `auth.users.id` ->
`profiles.person_id` -> `resolveNames()` through the caller's own client.

## 4. Decision

`club_admin` approves or rejects (`registrations_admin_update`;
`decided_at`/`decided_by` stamped by the trigger, `decision_note` free text).

**Approval with a `team_id` creates the live `team_memberships` (player) row
for the season.** That means P2.1's SG-6 composition guard runs at the moment
of approval: approving a child onto a team whose coach has no in-date DBS /
safeguarding qualification **fails**, naming the coach, and the registration
stays pending. The screen must surface that message verbatim — it is the
administrator's to-do list, and the only correct fix is a certification, an
exemption from the `safeguarding_lead`, or a different team.

The subject (adult) or an active guardian (minor) may **withdraw** a
**pending** registration (`*_withdraw` policies — the only update they can
make, and only to `withdrawn`). Once it has been APPROVED only a `club_admin`
may withdraw it (Adam, 2026-08-25: "parents can't withdraw registration after
it's been granted, only admin"); `registrations_guard()` refuses anyone else
with a readable P0001 naming the administrator, and /family and
/my-registrations offer the button on pending rows only. The rule lives in the
guard rather than in the policies' `USING` clause on purpose: a policy that
hides the row cannot explain itself. Withdrawal does not end an existing team
membership; that is an administrative act.

**A new registration notifies the desk (`20260825230000`).**
`registration_pending_notify()` — AFTER INSERT, statement-level, in-app only
(`public.notify()`), every live `club_admin` except the person who submitted it,
linking to /registrations. `account_request_arrival_notify()` does the same for
a new `account_requests` row, linking to /approvals. No email: Adam's standing
rule. The pre-existing `account_requests_notify()` (the DECISION, back to the
requester) is untouched.

## 5. Audit

`registration.submitted` and `registration.decided` on `public.audit_log`,
detail `{person_id, season_id, team_id, status}`.

## 6. Open for Adam

- Whether registration should require `dob` for everyone (D1). The DB permits
  unknown DOB (treated as a minor everywhere), which makes an adult with no
  DOB un-registrable by themself — they need a guardian or an admin. Adam's
  answer to D1 decides whether the screen makes DOB mandatory.
- Payment at registration (subs) is P4.1; the form has no money fields.
