# Decisions log

Record of decisions made with Adam that are not derivable from the code. Newest last.
Format: date · task · decision · why.

| Date | Task | Decision | Why |
|---|---|---|---|
| 2026-08-21 | §3 | Decisions Q1–Q6 as recorded in PLAN.md §3 (Neon auth unknown → discover in P3.1; fixtures-system untouched, FA Full-Time import instead; USC-Sale out of scope; Supabase Storage for media; cutover ASAP after P3.3 gate; Cheshire FA safeguarding framework). | Resolved in planning session. |
| 2026-08-22 | P0.2 | Baseline migration reconstructed from live prod catalogs (not `db pull`) and marked applied with `migration repair`; prod's five functions re-saved with LF via a no-op migration pushed with Adam's approval. | Prod had no migration history; CLI credentials were unavailable when the baseline was authored. CRLF bodies made `db diff` permanently noisy. |
| 2026-08-22 | P0.3 | CI proves migrations by replaying them on a fresh local Postgres in GitHub Actions plus a `db push --dry-run` against prod; no per-PR Supabase preview branches. | Equivalent rehearsal, no branch billing, no hosted project touched. Preview branches remain the tool for rehearsing specific risky migrations by hand. |
| 2026-08-22 | P0.4 | Import the function-room app into `apps/web` now. Leave the existing Vercel project and the `roombooking.aomsportsclub.co.uk` domain untouched until the platform is ready to cut over. | Single repo simplifies the P1.6 data-layer rewrite; the live site keeps serving from its current deploy meanwhile. |
| 2026-08-22 | P0.5 | Adopt Linear (workspace `th1878`, free plan) as the issue tracker via the claude.ai Linear connector: one project per phase, one issue per PLAN.md task, labels `migration`/`rls`/`safeguarding`/`human-review`. PLAN.md stays the source of truth for task definitions and status. | Adam had no tracker; chose Linear over GitHub Issues or PLAN-only tracking. |
| 2026-08-22 | P1.1 / P1.2 | Prod pushes batched: `20260822100000_people.sql` (P1.1) is applied to prod together with `20260822110000_profiles_person_link.sql` (P1.2), after P1.2's review — not on P1.1's merge. | P1.1 leaves `public.people` empty and unreferenced; P1.2 is what populates it and links it to `profiles`. Pushing them together means one write window, one backfill, one reconciliation, and no interval in which prod carries an empty table whose only purpose has not arrived yet. |
| 2026-08-22 | P1.2 | **Never auto-link a new login to an existing `people` row by email.** `handle_new_user()` and `profiles_ensure_person()` always create a new person; neither looks up `people` by email. Merging two people is an administrative act needing a human decision and gets a deliberate tool later (P1.4+). | Families share an email address, and P1.1's design has a minor's contact route running through their guardian, so a child's record may legitimately carry a parent's address. Matching on it would hand a brand-new account somebody else's identity together with every guardianship, role, certification and conversation attached to it. A duplicate person is a tidy-up; a wrongly merged person is a safeguarding incident. |
| 2026-08-22 | P1.1 / P1.2 | The batched prod push happened: `20260822100000_people.sql` and `20260822110000_profiles_person_link.sql` applied to `rwpglslbkhsqyxjhnpue` together on 2026-08-22. Migration history 4/4 in sync, `supabase db diff --linked -s public` empty, backfill linked all 39 profiles to 39 people with one `migration.backfill` audit row. Both PLAN rows are now ☑. | Executes the batching decision recorded above: one write window, one backfill, one reconciliation. |
| 2026-08-22 | P1.3 | **Guardian access to a child's `people` row lapses when the child stops being a minor** — `people_guardian_read` carries `public.is_minor(people.id)` alongside `ended_at is null`. This is Open Decision **D5**'s default, not its answer. | SG-4: "Its effects lapse: guardian access to the young person's data ends at 18 (or at a transition age — Open Decision D5), which is enforced in the reading policies, not by mutating the guardianship row." D5 is unresolved and needs Adam's view; the invariant's own stated default is implemented meanwhile. Changing it is a one-policy migration. The same decision settles whether a 16–17 year old should see the guardianship rows held over them, which is why no child-read policy was written either. |
| 2026-08-22 | P1.3 | **`guardianships` rows may be hard-deleted; `deny_hard_delete()` is deliberately NOT attached.** `DELETE` is granted to `authenticated` and `service_role`, with a committee `FOR DELETE` policy. `TRUNCATE` is refused (`deny_truncate()` + revoke). | SG-2's statement names four tables and this is not one of them. More decisively, SG-1.8 requires the delete to remain reachable and says why: "a flat reject makes a mistyped or genuinely-ended guardianship permanently unremovable, which is itself a data-protection problem." The guard on deletion is SG-1.8's conditional one (close the affected conversations first), not SG-2's unconditional one. `service_role` keeps the privilege because SG-1.8's tests are specified to run as `service_role` — meaningful only if the trigger, not the grant, is what refuses. TRUNCATE is refused as a **strengthening** (SAFEGUARDING §6.2): a row-level BEFORE DELETE trigger never fires on TRUNCATE, so one statement would strip every guardian link in the club with the SG-1.8 guard never consulted. |
| 2026-08-22 | P1.3 | **`ended_at` added to the SG-1.8 guard's watched column list**, beyond the `guardian_person_id` / `child_person_id` / DELETE that SG-1.8 names. | SG-1.8 was written before this table had a soft-end column. A retired link cannot be what makes an adult↔minor 1:1 permissible, so ending a link is indistinguishable from deleting it for SG-1 purposes — and omitting it would leave a hole with a soft edge: an administrator blocked from deleting a row could simply end it and reach the same prohibited state. A strengthening, permitted by §6.2 without prior agreement but recorded here. P5.2's completed body must honour it. |
| 2026-08-22 | P1.3 | **The `(guardian_person_id, child_person_id)` unique index is PARTIAL — live links only (`where ended_at is null`)** — rather than SG-4's literal `UNIQUE (guardian_person_id, child_person_id)`. **Needs Adam's confirmation under SAFEGUARDING §6.2.** | SG-4 predates this table having `ended_at`. A full UNIQUE plus a soft end means an ended foster placement could never be recorded again for the same carer, and a resumed arrangement could only be expressed by clearing `ended_at` on the old row — erasing the gap, which is exactly the history a safeguarding record exists to hold. The partial index keeps SG-4's intent (no two simultaneous live links for one pair, so "is X the guardian of Y?" has one answer) while making the honest record possible. Not equivalent to the literal rule, so flagged rather than treated as a free refinement. |
| 2026-08-22 | P1.3 | **The SG-4 age guard fires on INSERT and on retarget only**, not on every UPDATE — deviating from the task description's "BEFORE INSERT OR UPDATE". | SG-4 scopes its statement to creation ("valid only if, **at creation**") and repeats it: "The link outlives the minority… the age tests apply at creation only." A whole-row guard would refuse an `ended_at` or `notes` edit the day after the child's 18th birthday and freeze the row forever. SAFEGUARDING §1 settles the conflict: "Where this document and a task description disagree, this document wins." Same precedent as P1.1's refusal of `people_staff_read`. |
| 2026-08-22 | P1.3 | **Known open SG-7 gap:** committee reads of `public.guardianships` are not audit-logged. Recorded, not closed. | SG-7 requires an audit row for "any read of `guardianships` outside a guardian's own links". Postgres has no SELECT trigger, so SG-7's own enforcement note makes such reads `SECURITY DEFINER` accessor functions with no direct table grant behind them — a convention that arrives with P4.3 along with `public.write_audit()`. Inventing it two tasks early would fix the shape of the audited-accessor pattern before the task that owns it. |
| 2026-08-22 | P1.4 | **The legacy `public.user_role` → `public.app_role` mapping is: `super_user` → `club_admin`; `committee` → `club_admin`; `bar_manager` → `staff`; `bar` → `staff`; `staff` → `staff`; `member` → `member`.** One shared IMMUTABLE function (`map_user_role_to_app_role`) owns it, called by both the backfill and the `profiles` sync trigger; the mapping is also written into the backfill's audit row. `app_role` is a **new** type, not an extension of `user_role`, which is untouched. | The two enums answer different questions. `user_role` is the old app's account-type column ('bar', 'bar_manager', 'super_user'); `app_role` is a statement about a person's standing in the club under SAFEGUARDING §1.3. Merging them would mean polluting the safeguarding vocabulary with 'bar' or teaching the old app about 'safeguarding_lead'. `bar_manager` maps to `staff` and not to anything more privileged because §1.3 defines `staff` as "Bar, clubhouse, ground staff" with "no inherent access to member or child data" — managing a bar rota is not member-data authority. Two functions rather than one so the mapping stays a readable CASE while the fail-loud on an unmapped future value lives in a `_strict` wrapper: a NULL would silently mean "this person gets no role at all". |
| 2026-08-22 | P1.4 | **NOBODY is granted `safeguarding_lead`, `coach`, `parent` or `hirer` automatically.** `safeguarding_lead` exists with zero holders until Adam assigns it by hand. | It is Open Decision **D9**. SAFEGUARDING §1.3 calls it "the only role whose *purpose* is safeguarding" and SG-3 makes it the sole writer of case notes; deriving it from `profiles.role in ('committee','super_user')` would hand every committee member the Club Welfare Officer's readership of every concern in the club — including concerns about themselves, which SG-3's write model excludes in terms ("A person named in a concern must not be able to read it, even if they are a `club_admin`"). Zero holders is the correct failure mode: SG-3's reads return nothing rather than the wrong thing. `coach` cannot be derived (no teams until P2.1). `parent` is a label, and §1.3 is emphatic that authority comes from the guardianship *link*, never the role — deriving it from `guardianships` would create a second, staler source of truth. `hirer` is "deliberately isolated" and the old app's hirers are not `profiles` rows at all. |
| 2026-08-22 | P1.4 | **`person_roles` is soft-revoke only: `revoked_at`, plus `deny_hard_delete()` AND `deny_truncate()`, DELETE/TRUNCATE revoked from all three API roles, and no FOR DELETE policy.** A re-grant is a new row, so the gap between the two survives. Deliberately the opposite of P1.3's position on `guardianships`. | Not an inconsistency: P1.3's reason is absent here. SG-1.8 required the guardianship DELETE to stay reachable ("a flat reject makes a mistyped or genuinely-ended guardianship permanently unremovable, which is itself a data-protection problem"); a role granted in error is instead corrected by revoking it, which leaves an honest record and no stuck row. And the history is load-bearing: every SG-7-logged safeguarding access is authorised by a row in this table (SG-3 restricts concerns to "holders of `safeguarding_lead` or `club_admin`"), so deleting a grant would leave an `audit_log` row saying a person read a concern while the database says they never had the standing to. SG-6's compliance history has the same dependency. This extends SG-2 to a table its statement does not name — the same move P1.1 made for `people`, permitted as a strengthening by §6.2 and recorded here. A member does not see their own *revoked* roles either: "you used to be a coach" should come from a human, not a role list. |
| 2026-08-22 | P1.4 | **The `profiles.role` → `person_roles` sync trigger is ONE-DIRECTIONAL.** Nothing propagates back. The reverse direction is P1.6 / Phase 4, which moves the apps/web role screens onto `person_roles`, makes `profiles.role` derived-or-dropped, and deletes this trigger. `handle_new_user()` is deliberately **not** changed. | Writing back cannot be expressed (`profiles.role` is one value; a person holds several roles — which of {club_admin, coach, member} would be written?), `app_role` has values `user_role` has no equivalent for, and a write-back would make the old app's ~38 `is_committee()` policies respond to grants made in the new model — precisely the sweeping change P1.4 says it is not making. `handle_new_user()` needs no change because an AFTER INSERT trigger on `profiles` fires for a trigger-issued insert like any other; adding it there would be a second code path to keep in step for no gain (asserted by test, not assumed). `granted_by` is left NULL by the trigger — the grant was made by a machine translating a legacy column, and attributing it to whoever clicked "make committee" would be a small lie in an audit trail; the SG-7 row still carries `auth.uid()` as the actor. |
| 2026-08-22 | P1.4 | **The P1.1/P1.3 committee policies are dropped and recreated under `_admin_` names, with different splits on the two tables: `people` = read for `club_admin` + `safeguarding_lead`, write for `club_admin` alone; `guardianships` = read AND write for both.** Confirmed with Adam 2026-08-22. | §1.3 gives `club_admin` "full administrative access" and describes `safeguarding_lead` as "Sole read/write of concerns alongside club_admin" — a safeguarding write remit, not member administration, so editing a member's name/dob/address stays with `club_admin` (the lead's `legal_hold` write arrives column-scoped with P4.3). SG-4, by contrast, states the guardianships rule literally — "club_admin and safeguarding_lead may SELECT and write all" — and then makes that write access load-bearing: "Their write access is deliberate and is exactly why SG-1.8 exists". SAFEGUARDING §1 makes the document outrank the task description, which had asked for lead-writes-concerns-only; narrowing it would quietly re-specify SG-1.8's premise. The rewrite is additive in effect because the backfill grants `club_admin` to exactly the set `is_committee()` was true for, in the same transaction — which is also the caveat for the rehearsal: skip the backfill and committee members lose their `people` read. |
| 2026-08-22 | P1.4 | **The backfill writes ONE summary `migration.backfill` audit row, not one per grant**, by suppressing the SG-7 trigger through a transaction-local GUC (`app.person_roles_audit_suppressed`, `set_config(..., is_local => true)`) that is read in exactly one place. | SG-7 wants role changes attributable, but 39 identical `role.granted` rows attributed to no actor, all at the same instant, would bury the grants a human actually made in the same table an auditor reads. The summary row carries the counts *and the mapping it applied*, so the 39 grants stay fully explained by one row. The GUC cannot outlive the transaction even if the function raises, and the tests assert both halves — no per-grant rows during the backfill, and an ordinary grant immediately afterwards still logged. |
| 2026-08-22 | Safeguarding / P1.7 (a) | **Children may hold their own app account where a guardian consents.** The minimum account age is **admin-editable in the app, default 13**. Up to age **14** a guardian must still be a participant in any adult↔child conversation — SG-1 is unchanged below the threshold and no consent can waive it. From **14** (also admin-editable), **with a guardian's `unsupervised_messaging` consent**, a child may message adults 1:1. Such conversations are flagged `supervised_by_lead`, and the safeguarding lead / `club_admin` may open or export **any** conversation involving a minor through a `SECURITY DEFINER` accessor that audit-logs each access (conversation id, actor, reason) — **not** by copying message content into `audit_log`. Written up as SAFEGUARDING.md §1.5 (SG-0.1/SG-0.2), SG-1.9, SG-9 and SG-10; implemented by the new task P1.7 and by P5.2. Open decisions D10 (what happens when the account age is raised), D11 (floor of 13 on the account age) and D12 (whether the messaging consent expires) are recorded, not settled. | Young referees need to communicate with adult officials, and requiring a parent in every exchange about a kick-off time or a postponement is not workable. FA guidance still requires supervision for younger children, so the exemption is bounded three ways — by an age, by a per-purpose revocable consent, and by the lead's logged visibility — rather than being a general relaxation of SG-1. A logged accessor rather than mirroring messages into `audit_log` because SG-7 forbids `detail` carrying the content whose access it records: the audit row must evidence the access, not duplicate the conversation into a table with a wider readership. |
| 2026-08-22 | P1.4 | **`20260822130000_roles.sql` pushed to prod** on 2026-08-22 after PR #9 merged. Migration history 6/6 in sync, `supabase db diff --linked -s public` empty, backfill granted 5 `club_admin` / 1 `staff` / 33 `member` across the 39 profiles, one `migration.backfill` audit row, nobody granted `safeguarding_lead`. The PLAN row is now ☑. | Executes the plan recorded above. The backfill and the `people_*` / `guardianships_*` policy rewrite run in the same transaction, so the window in which a committee member could have lost their `people` read never existed. |
| 2026-08-22 | P1.7 (b) | **`granted_by` on a `guardian_consents` row must be the guardian named on that row, or a `club_admin` — never a `safeguarding_lead`, and NULL is permitted.** There is still no admin INSERT *policy*: an administrator records a consent through `service_role`, and `guardian_person_id` must be a real, actively-linked, adult guardian. | SG-10's RLS sentence gives the grant to the guardian, and §13 implements exactly that for `authenticated`. But consent is overwhelmingly given on paper at registration and typed in by an administrator; refusing that would either push the club into logging in as the parent — destroying the attribution the row exists for — or leave `granted_by` empty on most real rows. `safeguarding_lead` is excluded for P1.4's reason: the role whose purpose is oversight must not be able to manufacture the permission it oversees. NULL stays legal for the Phase 3 import, exactly as `person_roles.granted_by` and `guardianships.created_by` already are — attributing an imported consent to whichever login ran the import would be a small lie in the record that matters most. |
| 2026-08-22 | P1.7 (c) | **`safeguarding_lead` may REVOKE a consent, alongside `club_admin`** — the task description said `club_admin` only. Needs Adam's confirmation. | SG-10 reads "`club_admin` and `safeguarding_lead` read all **and may revoke**": one subject, two verbs. SAFEGUARDING.md §1 settles the conflict — "Where this document and a task description disagree, this document wins" — and it is the same precedent P1.4 followed when SG-4 gave both admin roles write on `guardianships`. Right on the merits too: a Club Welfare Officer who learns an unsupervised-messaging consent should not stand must be able to withdraw it that minute, not after finding a committee member. Granting is a different matter and stays closed to both (see (b)). |
| 2026-08-22 | P1.7 (d) | **`has_active_consent()` checks revocation and expiry only. It does NOT re-check that the granting guardian still holds an active guardianship** — that is a grant-time condition. | Exactly the shape SG-4 uses for its age rules ("the age tests apply at creation only"). Re-checking at read time would mean ending a guardianship silently invalidated a child's app account with nothing happening in `guardian_consents` at all — and SG-10 is emphatic about the opposite direction: "Revoking `app_account` consent does not delete the account… a trigger that did it as a side effect of a consent edit would be a worse outcome than the gap it closes." A guardian who wants the consent gone revokes it: one column, one audit row, one visible act. |
| 2026-08-22 | P1.7 (e) | **A consent row's identity columns are immutable for every role including the owner** (`child_person_id`, `guardian_person_id`, `consent_type`, `granted_at`, `granted_by`, `notice_version`), **a revocation cannot be undone or altered in place**, and **a caller holding neither admin role — `service_role` and the owner included — may change only `revoked_at`/`revoked_by`.** | The table is evidence. Retargeting a consent, or quietly changing which notice the guardian was shown, is not a correction but a rewrite of the record a safeguarding enquiry relies on; a consent entered against the wrong child is revoked and re-granted, which leaves both facts visible. SG-10 already assumes this: `consent_after_revocation_can_be_granted_again` is a *new row*, which the partial unique index permits precisely because the old one is revoked. The non-admin restriction is fail-closed: an Edge Function has no legitimate reason to rewrite a consent's notes, and if one ever does it will go through a named accessor with its own audit row, on the SG-3/SG-9 pattern. A strengthening under SAFEGUARDING §6.2. |
| 2026-08-22 | P1.7 (f) | **`expires_at` ships present and honoured but always NULL, pending D12**; and **the SG-10 `profiles` guard also fires on `UPDATE OF person_id`**, which SG-10 names only for INSERT. | SG-10 asks for the column in terms — "left null until D12 is settled; `has_active_consent()` treats a past `expires_at` as inactive from the day it exists, so settling D12 later is a data change rather than a schema change". The UPDATE arm is a strengthening (§6.2): repointing a profile at an ineligible minor reaches exactly the state the INSERT arm exists to prevent, with nothing happening in `people` or `guardian_consents` — the SG-1.8 shape of hole — and it is a reachable path, since `profiles_person_link.test.sql` itself updates `person_id`. |
| 2026-08-22 | P1.7 (g) | **`public.people_dob_guard()` becomes SECURITY DEFINER**, and its platform-default EXECUTE grants are revoked from all three API roles. `guardian_consents_grant_guard()` and `profiles_account_eligibility_guard()` are SECURITY DEFINER for the same reason — unlike P1.3's `guardianships_guard()`, which stays SECURITY INVOKER. | The new SG-10 block asks "does this person hold an account?" by reading `public.profiles`, whose policy is `id = auth.uid() or is_committee()`. A `club_admin` who is not also `profiles.role = 'committee'` — a person P1.4 makes possible and P1.6 makes ordinary — would see only their own profile, the `EXISTS` would answer false, and the safeguarding re-check would be skipped without a word. A safeguarding guard must give the same answer whoever asks; a silently-skipped one is the worst available outcome. The consent guard has the mirror-image problem: a *parent* writes that table, and RLS-scoped reads of `people`/`guardianships` could make it refuse a guardian who does hold the link. P1.3's guard needs none of this because only administrators write `guardianships`. |
| 2026-08-22 | P1.7 (h) | **The adult-with-known-dob test is duplicated in `guardian_consents_grant_guard()` rather than factored out of `guardianships_guard()`.** | Factoring it would mean rewriting a P1.3 function whose exact `P0001` message text `guardianships.test.sql` asserts. The duplication is four lines; the alternative is a change to a tested safeguarding trigger in a migration with no other reason to touch it. If a third caller appears (P2.2 is the candidate), factor it then, in a migration whose subject it is. |
| 2026-08-23 | P1.5 | **Unified `bookings` stores a half-open `[starts_at, ends_at)` in `timestamptz`, not the legacy `date + start_time + end_time`.** Buffers are `pre_buffer_minutes` / `post_buffer_minutes`; the blocking window `[blocked_from, blocked_until)` is maintained by a BEFORE trigger and is what `bookings_no_overlap` indexes. | The legacy "end earlier than start means tomorrow" rule is a function-room idiom that makes no sense for a pitch allocation derived from a Full-Time kickoff. `timestamptz ± interval` is only STABLE, so neither a generated column nor an index expression can compute the buffered window — two trigger-maintained columns are the honest way to get an IMMUTABLE range into the GiST constraint. P1.6 converts with `(date + time) at time zone 'Europe/London'`. |
| 2026-08-23 | P1.5 | **The function-room pricing/extras/reminder columns are carried on `resources` and `bookings` as typed nullable columns, not folded into jsonb.** The generalised payments table is named `public.payments`. | P1.6 has to move a live app onto these tables; a column-for-column mapping is reviewable, a `details jsonb` hides it. A pitch simply leaves the room-only columns NULL. `payments` rather than `booking_payments` because the legacy table keeps that name until P1.6 renames it, and P4.1's subscription ledger is this table with `booking_id` made nullable, not a second table. |
| 2026-08-23 | P1.5 | **Bookings/payments RLS: `has_any_role(array['staff','club_admin'])` for read/insert/update, `is_club_admin()` for delete, plus a new booker self-read keyed on `booker_person_id = current_person_id()`. Every non-public policy is scoped `TO authenticated`. No SG-2 guard.** | Mirrors the legacy `is_staff()` / `is_committee()` split in the P1.4 model (P1.4 mapped every bar/bar_manager/staff profile to `staff`). Within SAFEGUARDING §1.3: a booking is a hire record and a hirer is "deliberately isolated". `TO authenticated` is load-bearing — policies OR together and the role helpers have EXECUTE revoked from `anon` by name, so an unscoped admin policy turned every anon read of an inactive resource into "permission denied for function" (found by pgTAP, fixed before rehearsal). No SG-2 guard because bookings are not in SG-2's list and the staff app hard-deletes cancelled enquiries today; whether `payments` becomes append-only is P4.1's call. |
| 2026-08-23 | P1.5 | **`20260823100000_resources_bookings.sql` pushed to prod** on 2026-08-23 after PR #11 merged (CI green; rehearsed on preview branch `p1-5-resources`). Migration history 8/8, `db diff --linked -s public` empty. Not batched with P1.6. | Unlike P1.1/P1.2, nothing here is load-bearing for the live app: three empty tables and fifteen policies on them, no existing policy touched, so there is no window in which prod is in a worse state than before. P1.6 is large (data copy + app data-layer rewrite + a Vercel repoint that needs Adam's credentials), and keeping P1.5 out of that push keeps P1.6's reconciliation about P1.6's data. |
| 2026-08-23 | P1.6 | **P1.6 ships an idempotent `migrate_room_bookings()` sync and leaves the legacy tables live; the rename-to-`_legacy` + read-only views step is a runbook (`docs/runbooks/P1.6-cutover.md`) to be run with the Vercel repoint, not part of the migration.** | The public site still deploys from the old repo against this database (P0.4 deferral), and renaming `room_bookings` would break it the instant the migration was pushed. The old app's writes go on until the domain moves, so the copy must be re-runnable under a write freeze at that moment — upsert by `legacy_*_id`, delete what legacy hard-deleted, reconcile, then rename. Claude holds no Vercel credentials, so the cutover is Adam's step; everything up to it is automated and gated by `reconcile_room_bookings()`. |
| 2026-08-23 | P1.6 | **No `people` row is created for hirers without a login** (`booker_person_id` NULL for 10 of 40 prod bookings); the contact snapshot on the booking is the record. | A hire contract is a separate legal basis from membership; a hirer is "deliberately isolated" (SAFEGUARDING §1.3). Matching or creating people by email is exactly what P1.2 forbids. When such a hirer later signs up, `handle_new_user()` creates their person and the booking can be linked by an administrator — a tidy-up, never an automatic merge. |
| 2026-08-23 | P1.6 | **`public.audit_log` gains the SG-2 guard and `public.write_audit()` here**, though neither is in P1.6's task description. | SAFEGUARDING §4 lists both under "Audit (Phase 1 review)", P1.6 is the last Phase 1 task, and P1.6 is itself asked to "record the migration in audit_log" — the right moment to make that table append-only at every layer including `service_role`. The `audit_read` policy rewrite onto `person_roles` is left for the Phase 4 role move, with the rest of the `is_committee()` policies. |
| 2026-08-23 | P1.6 | **`20260823110000_migrate_room_data.sql` pushed to prod** after PR #12 merged (CI green; rehearsed twice with prod's real rows). Reconciliation on prod 20/20 ok; one `migration.backfill` audit row. Legacy tables untouched and still live. The cutover (`docs/runbooks/P1.6-cutover.md`) waits for Adam's Vercel access. | The copy is idempotent and nothing reads it in production until apps/web is the deployed site, so pushing early costs nothing and means the cutover evening is a re-sync + rename, not a first run. |
| 2026-08-23 | P2.1 | **`certifications` and `certification_exemptions` are created in P2.1, not P4.3**, so SG-6 tier 1 evaluates real rows from the first team. Required set fixed at fa_dbs + safeguarding_children (C3). No automatic `coach` app role from a coach membership. | SG-6 names P2.1 as the implementer of the triggers; a trigger over a missing table is either a stub (prohibited state reachable for weeks) or a blanket refusal (no coach on any youth team until Phase 4). P4.3 adds nudges, the nightly report and compliance views. The app-wide `coach` role is an administrator's deliberate grant (§1.3); RLS here keys on the live membership via `is_team_staff()`. |
| 2026-08-23 | P2.2 | **Photo consent is four values of `consent_type` on `guardian_consents`, not a `photo_consents` table.** Per-season scoping is `expires_at` = season end at capture. SG-7 vocabulary for photo consent is therefore P1.7's `safeguarding.consent.granted`/`.revoked`, not the proposed `safeguarding.consent.change`. | SAFEGUARDING §4's P1.7 block says in terms "P2.2 adds the SG-5 photo-consent values by `alter type … add value`"; the older `photo_consents(...)` checklist line predates P1.7 settling the consent model. One table gives one grant guard (active guardianship, adult guardian with known dob), one immutability rule, one audit trigger and one `has_active_consent()` for P4.5's consent-filtered views — four fewer places for the rule to drift. SG-5's "absence of a row = no consent" holds by construction. |
| 2026-08-23 | P2.4 | **The importer's reconcile rule lives in one SECURITY DEFINER function, `import_fixtures()`; the Edge Function and the manual paste/CSV path both call it.** Absence from a Full-Time page is NOT treated as cancellation; manual rows are never touched by an import. The nightly schedule is pg_cron → pg_net → `invoke_edge_function()`, reading the project URL and service-role key from **Vault** and doing nothing (with a NOTICE) until both secrets exist. | Two copies of "what does a known external_ref with a new kickoff mean" would drift; pgTAP can test one. Full-Time pages are filtered views, so a missing row is evidence of nothing. Secrets never belong in a migration; a cron job that fails at 03:00 for a missing secret helps nobody. |
| 2026-08-23 | P2.5 | **The fixture is the master of its pitch booking.** A reschedule MOVES the linked booking; a clash FLAGS (`allocation_conflict` + audit row) and leaves the booking where it was; a linked fixture booking cannot be cancelled/moved/deleted directly. | An importer run must not abort because one pitch is taken, and a silent double booking is the one outcome PLAN §6 forbids. The admin gets a named conflict and the fixture reappears in the unallocated view. |
| 2026-08-23 | P4.3 | **An unauthorised `read_concerns()` returns zero rows AND writes the SG-7 audit row; it does not raise.** `club_admin` reads concerns but never case notes; the subject of a concern is excluded from every read even as `club_admin`. | A raise rolls the audit row back with it, and SG-7 says "an unsuccessful fishing attempt is exactly what we most want logged". The rest is SG-3's write model as written. |
| 2026-08-23 | P4.4 | **SMS is opt-in; email/push/in-app are opt-out; `transactional` messages ignore preferences but never suppressions; `comms.dry_run` site setting stops delivery platform-wide.** | Texts cost money and nobody asked for them; a booking confirmation is not marketing; a bounced address must stay bounced whatever the category; P4.2's dry-run mode belongs to the whole platform, not one job. |
| 2026-08-23 | P4.5 | **`authenticated` has no SELECT on `media_items`/`media_subjects`; reads are `media_gallery()`/`media_export()`; untagged media fails closed; withdrawal quarantines the object so old signatures break.** | SG-5 in terms: "filter in the application is not acceptable". |
| 2026-08-23 | P5.1–P5.6 | **Phase 5 is BUILT on the basis of the decisions Adam made on 2026-08-22 (SG-1.9, SG-9, SG-10) but not yet SIGNED OFF: the P5.1 spec (`docs/specs/P5.1-messaging-spec.md`) and the §2.3 citations await Adam.** Messaging stays behind no feature flag in the schema; the web/mobile UI ships it to users only after sign-off. | PLAN §11 requires Adam's approval of the spec before build; the 2026-08-23 `/goal` instruction was to proceed without input. The reconciliation is SAFEGUARDING §2.3's own: the mechanism ships, sign-off waits, and nothing reaches a child until the citations are checked. |
| 2026-08-23 | P5.2 | **One SG-1 evaluation function with hypothetical parameters** (pair to ignore, consent to treat as revoked, new age, new dob) is what every trigger site calls, and `conversation_participants` is checked by a per-row AFTER constraint trigger (immediate, not deferred). **P5.3 inserts guardians before the minor** for that reason. | Deferred triggers fire at commit, which pgTAP's savepoint-based `throws_ok` never sees — a deferred SG-1 would have made every violation test pass vacuously. Immediate per-row checking means the sync must order its inserts so that a minor is never the second person in a room before their guardian. |
| 2026-08-23 | P5.6 | **Retention ships disabled and forced to dry-run** (`retention.enabled = 'false'`), redacts rather than deletes, skips legal holds and open concerns, never touches `audit_log`. | SG-8 and D7 in terms. |
| 2026-08-23 | Phase 4/5 | **Process deviation, recorded:** `supabase db push` run from the `phase-5/messaging` branch after PR #19 (Phase 4) merged applied Phase 5's five migrations to prod as well, before PR #20 was merged. All five had been rehearsed on preview branch `p5-messaging` (1148/1148, lint clean) and CI on PR #20 replays them; nothing was rolled back. | `db push` pushes every local migration file, not the merged set. Push from `main` after merging, never from a feature branch carrying later migrations. Noted in memory so it does not recur. |
| 2026-08-23 | P3.1 | **Phase 3 not started: the Neon pitch-booking source and a read-only Neon connection are not on this machine or reachable by the GitHub token, after a full search (recorded in the PLAN row).** Phases 4–6 were completed ahead of it. | CLAUDE.md makes Neon a read-only source and §3 Q1 makes discovering its auth mechanism the first step; neither can be faked. Attempting Phase 3 against guessed schemas would produce a MIGRATION_MAP with nothing behind it. The unblock is two inputs from Adam (`docs/runbooks/P3-unblock.md`). |
| 2026-08-23 | P3.1 | **Q1 resolved from the live application without credentials: the Neon pitch-booking app (`coaches.` = `membership.aomsportsclub.co.uk`) uses NextAuth with a single Credentials provider — local password hashes, no Clerk, no OAuth.** Evidence: `/api/auth/providers`, `/api/auth/csrf`, the `callbackUrl` redirect, the email+password login and reset flow. MIGRATION_MAP.md §1–§4 written on that basis; §5 waits for the read-only connection. | PLAN §3 Q1 asks for the mechanism "with evidence"; the running app is evidence the codebase would only confirm. The mapping is proposed, not applied — nothing can be imported without the data, and SG-0/SG-10 make the unknown-DOB question one Adam must settle before any user is created. |
| 2026-08-23 | P3.1 | **Neon source obtained (`TheHeathens1878/aom-fc-pitch-booking`, cloned to `C:/Projects/aom-fc-pitch-booking` at `6881cca`). Auth confirmed from code: NextAuth Credentials + JWT, no adapter tables, `bcryptjs` cost 12 (`$2a$12$`) → Strategy A hash import is viable; `my-family` "locked" child accounts (`nologin-…@placeholder.invalid`, random hash) get no login.** The app is far wider than pitch booking (62 Prisma models: family links, team chat, coach groups, lobby, waiting list, attendance) and has **no payments/Stripe/fixtures at all** — MIGRATION_MAP §4 rewritten per table with four decisions for Adam (D-P3-1…4). Row counts and data checks (§5) still need the read-only Neon URL. | Mapping must be written against the real schema, not the live-site inference. |
| 2026-08-23 | P3.1 | **Neon data audited read-only (role `claude_ro`, SELECT only): 61 users all `$2a$` bcrypt, 73 teams, 15 pitches/5 venues, 76 bookings with no overlaps, 73 waiting-list entries, 7 chat messages, 0 attachments, 0 medical/emergency/address data; all 56 adults have NULL `dateOfBirth`; 6 emails already exist in prod `auth.users`.** MIGRATION_MAP §5 written; chat, attendance and medical-data questions are moot; three decisions remain for Adam (D-P3-2 certification exemptions, D-P3-5 email matches = same person, D-P3-6 adult-DOB carve-out with first-login DOB gate). | Small dataset → the transform is simple; the safeguarding model (SG-0 NULL-DOB = minor) is the only real design question, and it is Adam's call. |
| 2026-08-23 | P3.1 | **Adam decided: 1a adult-DOB carve-out (imported adults attested by the club admin, DOB forced at first login), 2a coach memberships live with 60-day `certification_exemptions`, 3a exact email matches with existing `auth.users` are the same person.** | Unblocks P3.2/P3.3. |
| 2026-08-23 | P3.3 | **Import built around deferred activation, not a relaxed SG-0.** Adam's 1a ("treat attested adults as adults until DOB is entered") would have meant editing the minor test at ~10 guard sites across six migrations; instead imported adults keep `dob NULL`, are minors to every guard, and their guardianships/memberships wait in `neon_import_pending` until the first-login DOB gate (`complete_own_dob`, once, audited) supplies the date — at which point they are applied with a 30-day exemption where SG-6 needs one (2a). Same outcome for the user (one extra screen on first login); no change to any safeguarding rule. Known minors with an email are excluded from the auth import (P1.7 consent flow). Rehearsed on branch `p3-neon`: reconcile 27/27, 57/57 accounts linked, gate walked through. | SAFEGUARDING.md §1.2 fail-closed; SG-0; PLAN §2 human review on PR #27. |
| 2026-08-23 | P3.3 | **Legacy overlaps:** the Neon app allowed a venue closure over live bookings; the unified GiST constraint does not. The import inserts row-by-row and downgrades a clashing row to `cancelled` with an "OVERLAP AT IMPORT" note; `reconcile_neon()` fails if a booking or training session is downgraded, tolerates closures (two rows in the rehearsal, 18–26 Jul closure). | Data is kept, nothing silently dropped, humans review. |
| 2026-08-23 | P1.6 | **New Vercel project `club-management-web` created and linked to this repo (`apps/web` root) via the Vercel connector; production build green; env vars NOT set (connector cannot write them, Claude holds no secrets).** The old `aomsc-function-room` project is untouched and still serves `roombooking.aomsportsclub.co.uk`. | Gets the cutover pre-condition as far as it can go without secrets; Adam copies env vars from the old project, then the runbook window proceeds. |
| 2026-08-23 | Payments | **Adam: the club does not use Stripe; SumUp is the payment provider.** Applies immediately to the web app env (STRIPE_* left unset on `club-management-web`; SumUp keys regenerated for the new project). **Open (D-SumUp):** P4.1 subs (`stripe-checkout`/`stripe-webhook` Edge Functions, `stripe_events`, `stripe_*` columns on `subscription_plans`/`payments`, mobile `lib/checkout.ts`) and P6.2 were built against Stripe per PLAN §1 stack; SumUp has no subscription product equivalent to Stripe Billing, so a rework to SumUp hosted checkouts + our own recurrence needs a scoped decision before any code moves. | Recorded so the next session does not add more Stripe surface; rework deferred to an explicit decision. |
| 2026-08-23 | P1.6 | **Cutover migration `20260824100000_legacy_room_tables_to_views` pushed to prod** (PR #32). Sync + reconcile gate + rename run in one transaction, so the migration is the write freeze; the old Vercel project was *not* paused — it keeps serving read-only through the views until the domain moves. Merged by Claude although it touches booker PII: Adam instructed 2026-08-23 "complete the project without my intervention". Decommission of `*_legacy` no earlier than 2026-09-22 with a DECISIONS row. | Zero public downtime; any legacy write now fails loudly instead of drifting. |
| 2026-08-23 | P1.6 | **Public hostname is `portal.aomsportsclub.co.uk`** (Adam), not `roombooking.…`; the old hostname becomes a redirect on the new Vercel project. All app links derive from `NEXT_PUBLIC_SITE_URL`; only documented examples changed. | One hostname for the whole club app, not a room-booking-specific one. |
| 2026-08-23 | P1.6 | **P1.6 closed: app live on `portal.aomsportsclub.co.uk`, smoke test passed on the live hostname.** P0.4 acceptance ("works unchanged from monorepo deploy") met by the same deploy. | Cutover complete end-to-end. |
